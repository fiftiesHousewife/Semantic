# Backlog

An index of the plans under [`docs/plans/`](docs/plans). Each one states what it would measure, what would settle it, and what would end it; nothing is argued here.

Those documents are outside the corpus this library reads — [`.readingignore`](.readingignore) states so, and `StatedExclusions` is what honours it. A specification written in the vocabulary of a domain would put `ForeignExchange` and `InterestRate` into the tree and then find them.

New to the terms? [`docs/GLOSSARY.md`](docs/GLOSSARY.md) defines every linguistic and statistical word used here, with a reference for each. Library choices and their licences are in [`docs/DEPENDENCIES.md`](docs/DEPENDENCIES.md).

## What the codebase does, step by step

The library takes a local directory and produces a distribution over subject categories, plus a statement of which parts of the repository depart from that distribution further than chance would. Nine steps run in order. Each is named here with the classes that carry it, so a plan below can say which step it changes.

**1. The files are found and parsed.** `JavaSourceScopes` finds every `<module>/src/<set>/java` directory and `DocumentationScope` finds the markdown, both filtered by whatever `.readingignore` states through `StatedExclusions`. `ParsedRepository` parses each Java file with JavaParser and keeps only the names its author *declared* — a type, method, field, parameter, local, record component, constant, pattern binding or label. A name that is used here but declared elsewhere is somebody else's vocabulary, and the parse is the only thing that tells the two apart. `ImportOrigins` sorts each import into platform, own and third-party by asking `PlatformPackages`, which cites `ModuleFinder.ofSystem()`. Files that do not parse are counted and named, never dropped in silence.

**2. Each name is split into words.** `Tokeniser` splits on case transitions and separators; `IdentifierWords` applies it to a declared name and adds the acronym-run rule. A letter beside a digit is not a boundary, which is [UAX #29](https://www.unicode.org/reports/tr29/)'s own rule WB9/WB10 and not a gap. Where a compound carries no boundary at all, `WordSegmenter` enumerates candidate splits through `CompoundParses` and prices each piece with `PieceCost` against the frequency list `WordRanks` reads, so the commonest reading wins. This is grammar and it is permitted; a list of words to treat specially would not be. `CollocatedWords` then reads the words back in the order they were written: a run of adjacent words a topical resource publishes as one entry — `PublishedPhrases` holds the 69,713 the two bundled ones state — is one word from here on, and a run must begin and end on a word that carries subject matter, because the same dictionaries publish `to the` and `out of`.

**3. Each word is looked up.** `ContentWords` asks WordNet whether the word has an open-class entry, which removes the words English uses to hold a sentence together without a stop list existing. `WordMorphology` returns the dictionary form from the same query. `TopicCitations` then collects what two resources say the word is about: WordNet Domains labels each *sense*, and Wiktionary's topic vocabulary labels the *headword*. `StatedTopics` folds a label that another label of the same word already implies back into the label it came from, using the hierarchy the topic resource itself publishes.

**4. Each vote is weighted.** `SenseCoverage` gives the share of a word's senses the resource labelled. `WordSpecificity` gives `log(rank) / log(size)` against the frequency list, bounded in `[0, 1]` by the length of that list. `PhraseTopics` scores a subject by the geometric mean over the words of the name that agree on it, times the share of the name that agrees. Every weight is read off a published resource; none is chosen.

**5. The votes are pooled.** `TopicDistribution` accumulates one unit of evidence per phrase — one declared name, or one sentence of prose at half the weight — into a distribution over subjects for each scope and for the repository as a whole, over everything observed rather than over what was placed: what a phrase's words could not settle on a subject stays in the denominator, so a share says how much of what was written it is and not how much of what happened to resolve. `PooledLogOdds` and `AxisEvidenceAccumulator` do the arithmetic for a closed set of values, `OpenSpaceAccumulator` for an open one. `LegibilityReading` reports λ, the share of word occurrences any resource could be cited for, which is the denominator the rest rests on.

**6. Each scope is compared against the whole.** `JensenShannon` measures the distance between a scope's distribution and the repository's, in bits, with a per-topic decomposition that says which topics carried the distance. `PermutationNull` draws 999 resamples of a scope of the same size from the same repository, and `ChanceExpectedBest` sets the bar at the `1/(n+1)` quantile because every scope is tested at once. `Thresholds` holds the constants.

**7. The repository is placed in a published field.** `ArxivSubjects` supplies 152 subject descriptions, `PooledDescriptions` pools each subject's own prose through the same pipeline, and `SubjectPlacement` reports the nearest by divergence. Three schemes are placed against and none is picked: arXiv's categories, OpenAlex's topics, and CSO's, whose subjects `CsoSubjects` states by the labels of the topics beneath them because CSO publishes no prose. `SubjectNull` builds a taxonomy of chance and the nearest real subject must beat the nearest chance one. `OrdinaryEnglish` provides the same reading over the frequency list, as a baseline.

**8. Declared names are matched against a published term vocabulary.** `TermIndex` holds a taxonomy's concepts, already formatted as identifiers; `TermSpans` takes the longest term at each position of a name, left to right, with no two matches overlapping; `NormalisedTerms` reduces both sides to words, to dictionary forms, or to synsets, and reports each level separately. `TaxonomyTree` and `StatedSiblings` supply the publisher's own placement, and `CorroboratedTerms` accepts a single-word match only where the repository writes another concept from the same branch. **Nothing from this step feeds step 5.** It has only ever been run against this repository.

**9. The reading is written, as JSON and nothing else.** `ReadingSummary` keeps what cleared a bar and names what did not, and `ExportedReading` writes [`output/json/reading.json`](output/json/reading.json) — the answers, validated against `reading-export.schema.json` before the file exists. `ThemeGraph.evidence()` writes `evidence.json` beside it, which is the workings and which nothing validates. `ChangeFile` writes `changes.json` where the previous reading shares a `schemaVersion`, stating each figure before and after and naming every topic, word and concept that entered or left. `PublishedFormat` refuses any name that is not `.json`. All three are committed, so the current figures can be read without running anything.


## The live plan

[`POOL_THE_CORPUS.md`](docs/plans/POOL_THE_CORPUS.md) is where a session starts. Steps 2–4 landed on 2026-08-26: the pooled table carries a per-word error, a verdict rests on the margin between the claim and its error at reading schema 11.0, the occurrence floor is 63, and the backtest holds 34 of 36 expected results. What the plan still owes: the letter–digit boundary, which needs a catalogue to tell `utf8` from `col1` and holds 46.4% of the unsplit rows, and the two bounds still to be derived — the tail of the pooled table, and whether thirty repositories are enough. [`A_CORPUS_NOT_AN_INDEX.md`](docs/plans/A_CORPUS_NOT_AN_INDEX.md) holds the reasoning it rests on.

Beside it runs the consumer thread: one arm — the significant words — and pages that consume the published reading rather than the tree. The domain overlap page draws those words under four sources — WordNet Domains, eXtended WordNet Domains, arXiv categories and OpenAlex subfields — with `SubjectDomains` matching written words against each publisher's own subject account. Open in this thread: the weight-scaled XWND label voting landed and moved no leader on the nine, because the bundled reduction keeps one domain per synset, so the choice is top-k label vectors or a different reduction; and the discourse-guided second pass runs on the pages while the reading itself still weighs every sense by its tagged count alone.

## The queue

[`ONE_ANSWER.md`](docs/plans/ONE_ANSWER.md) starts the next session and its first three steps move no answer; beneath it the first three are ordered — 1 goes first because a red `read` is what every other figure is taken from — and everything below them is ranked, not scheduled.

| | Item | What is left | Blocked on |
|---|---|---|---|
| | Phrase evidence reaches the divergence arithmetic | [`PHRASE_EVIDENCE.md`](docs/plans/PHRASE_EVIDENCE.md) is the design; steps 1, 2 and 3b are landed and measured through 2026-08-28. 3b's bar permutes the *vocabulary* — `ScrambledTerms` deals a source's own words across its own terms. On the ten readings the leader is right on eight and BIAN fails on eight; the tail admits weak false positives, so no reading publishes the bar. **3a — the published term as the pooled unit — was built, measured on the nine and refuted on 2026-08-28, and the reading is reverted.** The unit is right for the term path and wrong for the prose arithmetic: the vocabulary ranking gained on eight members within the churn baseline, and the placement fell from 33 of 36 level readings standing apart to 22, because an arXiv subject is described in 29 units and the runs absorbed there are `machine_learning`, `natural_language_processing` and `programming_languages`. What is left: give a merged run the subject its own concept carries, so it arrives additive rather than silent, and re-run the same 36 levels | nothing |
| **next** | One answer, backed off | [`ONE_ANSWER.md`](docs/plans/ONE_ANSWER.md). The export publishes three unrelated blocks and the term arm carries no chance bar. The plan names one answer taken from the most specific evidence that stands above chance, and reports only what stands above chance beneath it. The two bars are the field's own two questions — unithood, which `PublishedPhrases` already answers by citation, and termhood, which nothing here has ever asked. The plan carries the approach class by class — `PooledRunShares` for the table, `SpecificTerms` wrapping the index before `ScrambledTerms` deals it, `AnswerRungs` for the cascade — with the test, command and criterion per step. **Step 1 landed 2026-08-28**: `corpusRunPool` pools the same hundred repositories in runs rather than words, under an index that states the term vocabularies' own labels as well as the dictionaries', and `reference-corpus-run-shares.tsv` bundles 1,200 runs over 17,797 occurrences — `time_zone` 446, `mime_type` 165, `resource_type` 51, `country_code` 11, and none of `cap_floor`, `fixed_leg`, `swap_leg`, `floating_leg`, `accrual_period`. `PooledRunShares` and `RunRanks` read it and no reading does, so no figure moved. **Step 2 landed the same day**: `WorkingJavaRuns` reads the table's own error column — the corpus has been shown to write a run where its share exceeds its own standard error — and `SpecificTerms` wraps a published index before `ScrambledTerms` deals it, keeping only the terms the corpus was asked about and did not write. Measured on all ten readings: 37 clears become 22, FIX still leads quickfixj at 10.4, FIBO leads strata and fineract, maven clears nothing at all where four vocabularies cleared before, and OLiA's `entity type` / `email address` / `phone number` clear on fineract is gone. FpML on tika falls 4.0 to 1.5 and still clears on six terms written once each, which is `MatchedPhrases` counting distinct terms. **Step 3 landed the same day, at schema 14.0 after two corrections**: `taxonomies[].bar` states the vocabulary's phrase count against what a deal of its own words reaches, a vocabulary appears only where it beats that, and `setAside` counts the rest. The placement is unmoved — 44 level readings, 39 apart, margin 0.0415, identical at every step — which is the criterion, because step 3 touches the term arm alone. **The two corrections are the interesting part.** The export judged only `MatchedTaxonomies`, so quickfixj could never publish FIX and strata could never publish FIBO whatever their counts; all seven bundled vocabularies are judged now, at a field of seven, and quickfixj publishes FIX at 10.4 times its bar, strata FIBO and FpML at 2.8, and maven still nothing. And `taxonomies[].concepts` carried the publisher's label but not the term the repository wrote, so a reading could not say which phrases were written — fineract published `capital` where the run was two words. A self read costs 2m 21s against 4m 5s and the backtest 37m against 16m: `TermOrderNull` is now the dominant term in a read. What is left: `PhraseSpecificity` reading runs against runs, the zero-valued bar three readings sit on, and the cascade. **Steps 4 and 5 have since landed**: the cascade fills `about` at schema 17.0 and was revised the same day to `summary.answers`, which publishes every source clearing the answering rung's bar rather than that rung's best one; and `ExportedReading.of(Path, String)` is the published entry point. **What is left is the half of step 4's criterion that failed, and the matched terms say the criterion named the wrong cases.** The placement holds — 35 of 36 level readings stand apart from chance. CSO answers correctly on the four readings the criterion expected silence from: `uni cast` and `udp` on aeron, `rsa` and `key agreement` on santuario, `hash functions` on besu, `parsing algorithm` on tika. **The defect is a finance vocabulary answering on ordinary English it uses as a class label** — FIX reads besu's `account` as a broker mnemonic and santuario's `issuer` as a bond issuer, FIBO reads tika's `index` as a reference shortcut, FpML reads quickfixj's `message` as an FpML message. The bar itself already counts only terms of more than one word; what it refuses to count is how often each is written, on the stated ground that a deal of a source states as many terms as the source does. **A raw occurrence count cannot replace it**: `THE_LITERATURE.md` §5.2 refuses log-likelihood, PMI, t-score and %DIFF on the line that a bound must follow from the statistic's definition, and an occurrence count is bounded only by how many names the repository declares. The bounded form is a share of the declared runs, which bounds itself at 1 and gives the identical verdict, because one repository held still is the denominator on both sides. That is the figure that separates: tika clears FpML on nine terms written 25 times between them, quickfixj clears FIX on 53 written 717 times, and the bar reads 9 against 53. **Placement does not separate and cannot be required** — 37 of the 94 multi-word terms behind FpML's correct strata answer are rootless, including `start date` and `valuation date`, because FpML declares its core types with no base type. A rootless match therefore counts fully towards the vocabulary and states nothing about a branch. **Measured on six members and this repository: every verdict counting how often changes is correct on the terms behind it** — tika loses FIBO (`responsible party` 5, `set window` 4), FpML (`contact info` 10, `packet header` 2) and CWE (`process control` 4, `infinite loop` 2); jpos loses FIBO (`merchant identifier` written once); santuario loses FIX (`card number` 4); quickfixj keeps FIX at 10.4 → 29.7 on `msg seq num` 160 and `begin string` 76; strata keeps FIBO, FpML and BIAN, all strengthened. besu losing CSO looks like a defect and is not: CSO reads besu on 5,435 single-word occurrences against 439 multi-word, and no single-word term has ever faced the bar. What is left is the seed-to-seed variance | nothing |
| | The pages take the shape of the answer | [`ONE_PAGE.md`](docs/plans/ONE_PAGE.md). Four views per repository with nothing stating which answers, which is the defect `ONE_ANSWER.md` fixes in the export, still present in the pictures. They are also stale at schema 13.0: `taxonomies` now holds only the vocabularies that beat their bar, so `term-trees` draws fewer and states no reason, no page draws a bar, and neither count step 3 added reaches a picture. The proposal is one page per reading — the answer, then a section per rung opening to its evidence — and one comparison across readings carrying the area each member's manifest states. Two tasks replace eight. It subsumes the word sources taking the phrase view's summary-and-drill shape | nothing |
| | The module codes get the names their publishers state | The trees and phrase areas group under module codes — FIBO's FND, FpML's shared and msg. Measured 2026-08-27: FIBO's pinned revision states a name per domain (MetadataFND.rdf labels FND "foundations domain"), so the FIBO extraction can carry names instead of codes; FpML's pinned schemas state no name for a file group, so its codes are the publisher's whole statement and a name needs a new pinned source. FIX fields are no longer flat: Orchestra states no `category` attribute on a field but does place it through the `fieldRef`s of the messages, components and groups that name it, and `FixFieldPlacements` reads that for 5,378 of the 6,203 | nothing |
| | A linguistics vocabulary beyond OLiA | Candidates measured 2026-08-27: LexInfo 3.0 (259 classes, CC BY 4.0 stated in the file, Apache-2.0 repo) with its OntoLex core (CC0 stated in the file) is the one clean ecosystem — noun phrase, collocation, morphological root, lexical concept, concept set, canonical form; GOLD is out at the licence gate; the NLP fields taxonomy (BSD-3, 83 fields, prose per concept) is placement-shaped, not matched. None states the measured gap words — lemma, lemmatisation, head word, content word, open class — so the bundle raises coverage without closing the gap. Bundle LexInfo + OntoLex per `adding-a-taxonomy` when a linguistics member joins the evaluation set, and measure then | nothing |
| | ~~A standard names itself where that distinguishes nothing~~ | **Landed 2026-08-31.** `DrawnReadings.about` printed every answering standard's own subject, so `computer science` sat on ten of the twelve about lines and separated none of them: CSO answers ten of the twelve readings and states that one subject for all ten. `DrawnReadings.subjectsAMajorityNames` now derives the bound the way `StatedAncestry.fieldLevels` does — a subject an outright majority of the readings drawn name is left out — and it refuses CSO alone, because FpML, FIX and FIBO answer five each. Narrow standards are untouched: tika keeps `software weaknesses` from CWE and CodeSemantics keeps `linguistic annotation` from OLiA. Three readings — jmeter-iso8583, aeron and maven — now state no standards line at all, which is the correct outcome, and a set of one reading refuses nothing because it has nothing to be told apart from. **The second defect this entry claimed does not exist**: `about` reads the already-sorted answer list, so strata's line was in beyond-chance order before the change | nothing |
| | ~~The export counts the vocabularies it set aside without naming them~~ | **Landed 2026-08-31 at schema 21.0.** `setAside.vocabulariesBelowTheirChanceBar` was an integer; it is a row per refused vocabulary carrying the publisher's name and the bar it failed — the count it reached, the count a deal of its own words reaches, the median, the field and the resamples. `taxonomies[].bar` and the refused rows share one `$defs/chanceBar` in the schema, and every vocabulary the reading judged is named in exactly one of the two lists. The readings page names them in a sentence: *Matched and set aside for writing no more phrases than a deal of the publisher's own words reaches: CSO 17 against 16* | nothing |
| | `atTheTopOfItsBranch` is empty where a publisher places a concept nowhere — awaiting its regeneration | **Landed 2026-08-30, and the figures it moves have not been written yet.** FpML's extraction reads `broader` off `xsd:extension` and 616 of its 1,405 rows state no base type, so the walk stopped at the matched concept and printed it back: `InterestAccrualsMethod` was reported at the top of an FpML branch because FpML declares it with no base type, not because FpML places anything above it. Measured over the twelve committed readings before the change: **175 of 1,624 concept rows named themselves** — FpML 109 of 305 (36%), FIX 47 of 202 (23%), FIBO 12 of 410, CSO 7 of 697 — and no row was ever empty. `StatedAncestry.topOfTheBranchOf` now answers `Optional<String>` and never the concept itself, which is the abstention rule: a signal that cannot speak abstains rather than voting zero. `rootOf` is unchanged, because a walk that must return a coordinate has to. `checkAll --no-build-cache` and the pinned findings are green. **What is left: `./gradlew read`, and `read -Dcs.clone.dir` over the evaluation members, to confirm that exactly those 175 rows go empty and no other figure moves** | nothing |
| | The readings page calls a parent a branch and a concept a phrase | `ReadingRow.branchesOf` groups the answering vocabulary's matched concepts by `placedUnder`, the one level the publisher states directly above each, and `ReadingsPage` labels the fold "%d branches, %d phrases". Neither word is the thing: a branch there is a single parent, and a phrase there is the publisher's concept label rather than the run the repository wrote. The count is right and the words are not | nothing |
| | ~~The ranking favours the largest vocabulary~~ — **landed 2026-08-31 at schema 22.0, and it did not do what was expected** | `ExportedTaxonomy.Bar` carries `atLeastAsExtreme` and `chanceRate` — `(atLeastAsExtreme + 1) / (resamples + 1)`, bounded in `[1/1000, 1]` by the estimator's own definition, which was computed on every reading and had no production caller. `DrawnReadings` ranks by the rate, ties broken by the count past the bar, because 999 deals cannot separate two counts no deal reached. **Measured on jpos: CSO 0.0010, FIBO 0.0080, FpML 0.0310, BIAN 0.0330, FIX 0.0380** — CSO's rate is at the floor, so no deal in 999 reached its 16 phrases and it is genuinely the rarest of the five. BIAN rises one place, past FIX, and no further. **The finding is that jpos leading with `public key cryptography` is not a ranking artefact**: CSO's match set really is the least likely to arise by chance, and the objection to it is about what a repository is *about* rather than about how rare its matches are. Nothing here asks that question | nothing |
| **next** | Describing a matched node | [`DESCRIBING_A_MATCH.md`](docs/plans/DESCRIBING_A_MATCH.md) holds the measurement over the 846 concepts the twelve readings matched at schema 22.0. **The repair: describe a match by the nearest node at or above it that its publisher describes, refuse a node an outright majority of the vocabulary sits beneath, and name the node the description came from.** 555 describe at the node, 229 by walking up, 17 stop at a field level and 45 have nothing anywhere — **93% describable**. Almost all of the walking is CSO's: it states no definition for any of its 11,438 topics and none of its ancestors do either, but `cso-abstracts.tsv` is already bundled with 5,294 Wikipedia summaries its own `owl:sameAs` names, read today only by the placement path. Joining it takes CSO from 83 describable to 296 of 337. The stopping rule needs no new bound — the walk degrades into `computer science`, which `StatedAncestry.fieldLevels` already computes by the majority rule. **It moves no figure**: a description enters no count, no bar and no placement, so it is verified by reading the pages rather than by re-reading the corpus | nothing |
| | ~~Favour the nodes with the better descriptions~~ — **refuted by its own measurement** | Proposed and measured 2026-08-31. **Length is not quality**: FIBO's definitions add a median of 17 words and are genus-and-differentia — `Fee` is *charge for services performed*, `AccountHolder` is *party that owns an account* — so a rule preferring length would rank those beneath BIAN's 75 words while they say more per word. **Restatement is already rare**: counting only the words a definition adds beyond its own label, pure restatement is 2% of FIX, 1% of FpML and 0% everywhere else, the real cases being `SubscriptionRequestType` → *Subscription Request Type* and `BondOption` → *A Bond Option*. **And the walk removes the problem the ordering was solving** — it was proposed when CSO read 20% coverage, and after the abstracts join 93% of matches are describable, so the ordering separates 7% of the field. What is left is to name the 45 undescribed and the 17 refused at a field level as undescribed, which costs no ranking rule | nothing |
| **next** | The description is ordered by evidential rarity, and the vocabulary that describes best comes last | Measured over the twelve published readings, 2026-08-31. **CSO contributes 651 matched concept rows, more than any other vocabulary, and 133 of them carry a definition — 20%.** BIAN carries one for all five of its rows at a mean of 90 words, CWE 5 of 5, FIBO 377 of 399, FIX 179 of 201, FpML 256 of 294. CSO also ranks first on most readings by chance rate, so **the vocabulary that dominates the description is the one that can least describe**: on jpos it displays `public keys` with no definition under `public key cryptography`, where BIAN displays `Card Capture` under `Cards` with 59 words, `Savings Account` under `Loans and Deposits` with 79, and `Point of Service` under `Servicing` with 101. `DrawnReading` already separates `answers` from `subjects`, but `DrawnReadings.subjectsOf` sorts the subjects by the source's own `chanceRate`, which imports the evidence ordering into the description. **This is not the move `DrawnReadings.formOf` refuses**: that refusal is about ranking an English label above stronger evidence, a judgement about label shape, where definition presence is a fact the publisher supplies and a zero-word definition describes nothing. **What settles it**: order the description by whether the publisher states a definition and by the share of the repository the concept accounts for, leave the answers block ordered by chance rate, and read whether any reading's stated area is reached that was not before | nothing |
| | Enrichment chosen for display, not for phrase counts | The three candidates surveyed on 2026-08-31 were each ranked by how many multi-word phrases they would add, which is the matching argument. **Under a display framing the order inverts.** BIAN's ~20,000 DDD entity names are the worst — `ATMNetworkOperatingSessionServiceProviderReference` describes nothing to a reader — and the best raw match. FIX's 4,445 code names are the best, because they are readable *and* carry an `xsd:documentation` synopsis each: `StopLimit`, `MarketOnClose`, `GoodTillCancel`. FpML's 358 unread view types sit between, at about 20 words of documentation each. **There is no display-only path**: a description is downstream of matching or placement, so every enrichment still enters through one of them and still moves all seven bars. That cost has to be paid by terms a repository actually writes, which is a matching question even when the motive is display | the ordering above |
| **next** | `FpmlConcepts` says naming a base is not climbing to it, and `StatedAncestry` climbs to it | Found 2026-08-31. `FpmlConcepts` deliberately keeps an `xsd:extension` base FpML writes in its own namespace even where this set holds no complex type for it — 195 rows extend `Scheme`, 32 `NonEmptyScheme`, 233 in all — and its javadoc states the reason and the rule: *“Naming the base is not climbing to it: a concept whose stated base this set holds no row for stands at its own root.”* **`StatedAncestry.of` climbs to it anyway**, because it walks `broader` and stops only when the lookup returns nothing, leaving the simple type in `pathAbove`. `ReadingRow.branchesOf` then groups by the last stated level, so the base becomes a subject on the page. **Live in the published readings: 38 concept rows, and the largest are not small** — fineract's `account type` ×510 and `payment type` ×499 group under *Scheme*, strata's `future id` ×34 and quickfixj's `exchange id` under *NonEmptyScheme*. This is the defect FIX's `Common` had and had fixed. **The obvious repair is refuted**: making the walk stop at any base the file holds no row for was checked across all seven vocabularies before being written, and it would take FIBO's 228 rows under `PartyRole`, `Classifier` and `Identifier` with it — parents in the OMG Commons Ontology Library that FIBO states deliberately, and that an earlier repair put there. The distinction is not whether a row exists but what the publisher declares the base to be: **FpML declares `Scheme` as an `xsd:simpleType`** and FIBO's `PartyRole` is a class, so the rule is citable from the schema set itself. That puts the repair in `FpmlConcepts`, which reads the same files, and therefore in the same re-extraction as the five unread views | nothing |
| | `fpml-terms.tsv`'s header describes behaviour the extraction deliberately changed | The header states that `broader` is *“dropped … where it names a type this file carries no complex type for, as Scheme and NonEmptyScheme are simple types.”* `FpmlConcepts` keeps exactly those, on purpose, and says so in its own javadoc. The file and the class disagree about what the file contains. `VocabularyProvenanceTest` checks a header states a source and a licence, not that it describes the rows beneath it — which is the same shape as a computed figure nothing reads and a diagnostic outside the gate: **the assertion exists and nothing runs it against reality** | nothing |
| | FIX and FpML placed against rather than matched | Proposed 2026-08-31. Both already state prose — FIX 7,003 of 7,170 rows at a median of 12 words, FpML 1,181 of 1,405 at 14 — so this costs a `PublishedSubjects` implementation and a `PlacedField` factory, with no extraction, no new pin and no licence question. **It is the test of why BIAN's placement failed.** BIAN pools to 43 subjects, which is too small a field for *something is always nearest* to bite; FIX offers 7,003 leaves and 242 pooled, FpML 1,181 and 153, which are the sizes arXiv (155) and OpenAlex (4,516) work at. **Two things have to be handled first, both found before writing any code**: FIX and FpML key `broader` by `prefLabel` where arXiv and OpenAlex key it by concept id, so `PooledDescriptions` calling `conceptOf(broader)` returns null — the same failure the BIAN placement hit; and FIX states several parents joined by `\|`, so the pooled level must take the first as `StatedAncestry.firstOf` already does. It also cuts against `TaxonomyShape`, which states a source cannot be matched and placed at once — so it is measured with the probe before any reading publishes it | the FpML parent defect above |
| | ~~FIX and FpML should point to BIAN service domains rather than compete with them~~ — **researched 2026-08-31 and refused: no publisher states the mapping** | The hypothesis was that the three financial vocabularies are layers rather than alternatives — FIX names messages, FpML names products, BIAN names what a bank does with them — so a FIX match could point at a BIAN capability instead of competing with it for the same bar. **Three published artefacts were looked for and the chain breaks at both links.** BIAN publishes exactly one external cross-reference: `bian-official/public` carries `apis-iso20022_ext-ddd/`, 259 YAML files at release 14 annotating each API attribute with an ISO 20022 business-component type — `PaymentTransaction → Payment`, `Amount → CurrencyAndAmount`, `Date → ISODateTime`. It is **attribute to type, never message to service domain**. `bian-official/artefacts`, the repository this project already pins, names no external standard in any of its 1,292 CSVs. The FIX Trading Community *states* it built message-by-message FIX↔ISO 20022 mappings, but the artefact is ISO/TR 22126-5:2022, which ISO sells, and none of the 41 repositories in their GitHub organisation carries it. FpML publishes nothing toward either; the nearest is FINOS CDM's FpML ingestion mappings, whose target is CDM and not BIAN, at an unverified licence. **The granularity would kill it even with the missing link in hand**: ISO types like `CurrencyAndAmount` and `ISODateTime` recur across nearly every service domain, so the pointer would point everywhere at once — the same failure the BIAN placement was just refuted for. Building it needs this project to author the correspondence, which the doctrine refuses. **Do not re-propose without a new published artefact** | nothing |
| | BIAN's own ISO 20022 annotations, as a second citable layer | Found 2026-08-31 while refusing the layering hypothesis above, and it answers a different question. `bian-official/public` at `release14.0.0/apis-iso20022_ext-ddd/` is Apache-2.0 and states, inside BIAN's own files, an ISO 20022 business-component type per API attribute across 259 service domains — so the ISO type *names* are quoted by a publisher this project already extracts, and reading them costs ISO nothing and needs no ISO licence. It would give BIAN a vocabulary of type names attached to named service domains where today it has only 319 capability labels, which is why it reaches six spans on a payments library. **It is a different repository from the pinned `artefacts`**, so it needs its own `ContentDigest` pin, and the licence must be read at that revision rather than assumed from the organisation. What settles it: extract the type names, match them, and read whether jpos and fineract gain phrases while the eight members outside banking do not | nothing |
| | ~~BIAN is prose and is matched as labels~~ — **built 2026-08-31 and refuted by its own control** | The resource's own header states the shape: *“nobody writes AccountReconciliation as a term of art in code.”* All 319 service domains state a role definition at a median of 57 words, so `BianServiceDomains` now implements `PublishedSubjects` and `PlacedField.ofBian` places at both levels. **The reading does not publish it.** Over the eleven evaluation members the business-domain level stood apart from chance on **nine**, including maven, tika, santuario, aeron and besu, and the two it could not separate were **fineract and strata** — the only banking and derivatives libraries in the set. Across eleven repositories it named two subjects: `Cross Channel` ten times and `Channel Specific` once. The likely cause is the size of the field: 319 domains pool into **43** business domains where arXiv states 155 and OpenAlex 4,516, so the null has too few subjects for *something is always nearest* to bite. The class and the factory stay, `FunctionalPlacementProbe` draws both levels, and a repair has to move that table before a reading publishes it. This is the second time BIAN's placement has failed its null | nothing |
| **1** | `./gradlew read` fails on a stale pinned figure, and the commit that moved it did not rerun the read | `ReachedSubjectTest.readsApacheTikasParsingAsLanguageBecauseCsoStatesItNowhereElse` asserts that `natural languages` is the branch Apache Tika reaches and that 11 of CSO's concepts sit written beneath it. **Measured 2026-08-31: it is 10, and the test fails on `main` at 3a7c493 with no working-tree change.** The fixture `/matched/tika-cso.tsv` has not been touched since 2026-08-17, so the matcher moved under it — which is exactly what the test's own javadoc says it is there to make visible. The candidate is 23256dd, which added `ExpandedRuns` as a fourth matching rung; the diagnostic now reports 162 runs recorded, 153 reaching a concept and 125 concepts after pooling. **`read` is tagged `diagnostic` and `checkAll` excludes it**, so the change passed the gate and 3d5b680 regenerated the readings without the pin being green. Settle it by naming the concept that left the branch before touching the number: retuning the pin first is marking your own homework | nothing |
| **2** | The reading cannot be argued with from the files it publishes | `evidence.json` carries the term path's workings and nothing about which word voted for what. It must carry each word's votes with their coverage and specificity, the words that agreed per phrase, a topic dropped with the rule that dropped it, and a run set aside recorded as a run. The `debugging-a-reading` skill holds the method until it does | nothing |
| | A tree that is not laid out as `<module>/src/<set>/java` reads as empty, in silence | `JavaSourceScopes` anchors every scope on that path, which is what keeps generated output out of a reading without a list of directories to ignore. A project laid out any other way therefore yields no scope and no declared name, and a reading of it is indistinguishable from a reading that found nothing to say. Measured 2026-08-28: `handcoded/fpml-toolkit-java` holds 195 Java files and 1.79 MB under `src-core`, `src-fpml` and `src-dsig`, and the reading reports 0 declared runs. It was refused as an evaluation-set member for that reason, so the library has no FpML-consuming member. What settles the repair: whether a scope can be found from what a build file declares its source directories to be — Maven's `sourceDirectory`, Gradle's `srcDirs` — which is a citation, where a sweep for `.java` is not | nothing |
| | The read spends four fifths of itself assembling the export | Measured 2026-08-30 with `readTimings` on this repository: the export stage held 176.9 s of a 222.7 s read and every other stage 46 s between them. No method stood above 6% — the cost was the same question asked of WordNet over and over, WordNet's own file reads taking about 15% between `readWord`, `getNextLineOffset` and `getIndexedLine`. **Three changes landed, each with a counting test red before it and the reading measured unmoved after.** `ContentWords.lemmaOf` asks the dictionary once per word rather than once per position of every candidate run: 222.7 s to 175.7 s. `WordNetSenses.entry` asks once per word per part of speech rather than once per question asked: 175.7 s to 140.1 s. `LongestRuns` bounds its walk by the longest run the index publishes beginning with the word standing there rather than by the index's own widest entry — 26 words, set by a single entry, against a mean of 2.3 over the 23,373 words that begin any run: 140.1 s to 123.8 s. **A read is 44% faster and no figure moved**, shown by reading tika before and after and diffing. `TermOrderNull` is no longer the dominant term: it runs on the common pool and its workers hold about 6% of the samples. What is left, from the profile after: `PublishedPhrases.runOf` at 6.3%, which spells a candidate key with `String.join` and discards it, and `PhraseTopics.lambda$of$4` at 3.8% | nothing |
| **3** | A package namespace that matches the published coordinate | The ported `io.github.fiftieshousewife.bi.lexicon` package disagrees with the coordinate the library publishes under. A mechanical rename across every module, landed before anything else moves the tree; `BundledResourceReachabilityTest` and `VocabularyProvenanceTest` say the resources still ship, because a resource path left behind fails at run time rather than compile time | nothing |
| | `WordSpecificity` gives the commonest word a weight of exactly zero | `log(rank)/log(size)` is 0.0 at rank 1 while the javadoc states the word still votes at the smallest expressible weight. `log(rank + 1)/log(size + 1)` was measured on a second frequency list and restored the silenced case with the bound still derived from the list's own length. Adopting it moves every figure in the tree, so it needs its own reading on the evaluation members | nothing |
| | `ContentWords` refuses a word shorter than three letters | The rule removes `id` for the wrong reason — `systemId` and its kin are the platform's spelling, and length is a stated bound where a derived one is required | nothing |
| | A sense weighed by what is written beside it | [`AGREED_SENSES.md`](docs/plans/AGREED_SENSES.md). `DomainOverlap.guidedByTheDiscourse` runs the two-pass division on the pages; the reading itself still weighs every sense by its tagged count alone, so `read` gives `publishing` its whole counted weight in a library that never means it. Gale, Church & Yarowsky's one sense per discourse and Yarowsky's confidence floor are the published grounding. Abandon if the evaluation members' rankings fall | nothing |
| | The `law` floor survives the coverage discount | `TopicTally` scales every phrase by credence and the licence vocabulary still floors the theme. The finding stands and the repair is unfound | nothing |
| | A topic vocabulary's own labels do not label themselves | `cryptography` is a topic the bundled vocabulary states and the word `cryptography` carries no topic: 470 of 719 labels | nothing |
| | `PooledDescriptions` joins one subject's last word to the next subject's first | The pooled level joins descriptions with a single space, and `CollocatedWords` reads a pair straddling the join as a phrase neither publisher wrote. Unmeasured: the number of collocations the joins create across the pooled levels | nothing |
| | The summary names a word and its plural as two findings | `word` 239 and `words` 136 both stand in the export's ten leading words. What settles pooling them: how much of the ranking's divergence the two spellings hold together against either alone, on the evaluation members | nothing |
| | The placement reads a linguistics library and a text-extraction toolkit as one category | Under both schemes tika and this repository read the same category — `cs.CL` and Natural Language Processing Techniques — while maven separates from both. The nine committed readings under [`output/`](output) are the evidence to answer whether that is a fault of the scheme or a fair reading | nothing |
| | FIBO named its parents by fragment, and two ontologies can spell one the same | **The dropped-heading half is fixed.** `FiboConcepts` used to write `broader` only where the superclass was one of the classes the file set carries, so a class FIBO places beneath the OMG Commons Ontology Library arrived unplaced. Counted at the pinned revision 119fa8c0 on 2026-08-29: **1,833 named classes, 46 stating no named superclass, and 291 stating one outside FIBO's own namespace** — Commons' PartiesAndSituations, Classifiers, DatesAndTimes, Collections and Identifiers between them. The extraction now carries the superclass FIBO states whichever ontology publishes it, which is what put `Identifier` back above `MerchantIdentifier` and a branch back into jPOS's answer. **The tree does not move**, because `TaxonomyTree.isRoot` already stands a concept at its own root where the file carries none of its stated parents, so `StatedSiblings` and `BranchAgreement` see exactly what they saw before and only `placedUnder` gains a value. **What is left is the collision.** `broader` names a parent by its URI fragment, and **63 of the 291 name a fragment FIBO itself also publishes** — Commons' `LegalEntity` above `Corporation`, Commons' `Organization` above `Syndicate`. Those 63 were never blank: the old rule matched the name and placed them under FIBO's concept of that name, which is usually but not provably the same concept. The fix inherits that. What settles it: whether the pairs are the same concept, which the two ontologies' own definitions answer where both state one, and whether `broader` should hold the identifier rather than the fragment for every source — a change that moves every taxonomy's tree and needs its own reading | nothing |
| | The pattern for extending to further taxonomies, and the candidates for it | [`EXTENDING_THE_TAXONOMIES.md`](docs/plans/EXTENDING_THE_TAXONOMIES.md). Ten files and two hand-written enumerations per vocabulary. The constraint that decides it: `TermOrderNull` passes `judged.size()` as the field, so **adding a vocabulary moves every other vocabulary's published bar on every reading** — which is why step 3, publishing the field's membership beside its size, runs before any addition. **Three fields were surveyed 2026-08-31**, one per repository whose unread runs reach no bundled resource: PRONOM (OGL v3.0) and EDAM (CC BY-SA 4.0) for tika's document formats, `ethereum/execution-specs` (CC0) for besu's distributed ledger, and the NIST CSRC glossary (US public information) with the IANA JOSE registries (CC0) for santuario's XML security. The licence gate refused the obvious standards-body answer in every field — the W3C Document License forbids the modification an extracted table is, ISO 22739 sells at the gate that already refused ISO 20022, and RFC 4949 grants verbatim redistribution of the memo and not extraction. **The strongest candidate in each field is not a glossary**, so each needs an extraction rule stating which of its names count as stated terms, and that rule is this project's choice rather than the publisher's | step 3, the field's membership |
| | A computing subject scheme that states prose | CSO is bundled and states no definition for any topic, so `CsoSubjects` compares labels and the placement stands apart on 2 of 22 level readings. Four candidates were surveyed on 2026-08-29 and three are refused on evidence. **ACM CCS** is the right shape and the right field and is refused at the licence gate — "educational and research purposes" is not redistribution. **EDAM** ([edamontology/edamontology](https://github.com/edamontology/edamontology), CC BY-SA 4.0) states 294 live topics and a definition for every one of them at a 17-word median, and they are bioinformatics: Proteomics, Lipidomics, Gene structure. It is a scheme for a bio member and for nothing else in the evaluation set. **SWO, the Software Ontology** ([allysonlister/swo](https://github.com/allysonlister/swo), CC BY 4.0) states 973 named classes and defines 511, and they are named software products — MATLAB, Microsoft Excel 2007, IntelliJ IDEA. That is a term vocabulary, not a subject scheme. **The one that stands is CSO's own `sameAs`.** CSO states 19,297 `sameAs` triples over 5,294 of its 11,438 topics, pointing at DBpedia, Wikidata, YAGO and Freebase; `CsoConcepts` reads none of them and its javadoc says `sameAs` "says nothing about what a topic is called", which is true and is beside the point once the topic needs prose rather than a name. Wikidata's descriptions are CC0 and its labels resolve from the entity ids CSO already states, so the join is the publisher's own link rather than a correspondence assembled here. What settles it: the share of the 225 placement subjects that gain prose, and whether the placement then clears its null on more than 2 of 22. `wikidata.org` is not on the session network allowlist, so the entity descriptions have to be fetched outside the sandbox and passed in with `-P`, the way every other extraction takes its source | a Wikidata fetch |
| | CSO reads from its own links, and it works | `CsoSubjects` states a subject as its labels **and** the Wikipedia summaries of itself and every topic beneath it that CSO links an article to — 46,477 words across the 225 placement subjects against 5,828 on labels alone, 134 of the 225 reaching prose where 86 are linked directly. **Measured on the eleven evaluation members on 2026-08-29, against a criterion stated before the run: CSO stands apart on 20 of 22 level readings, where labels alone managed 2.** arXiv manages 18 of 22 and OpenAlex 21 of 22, so the scheme that was the weakest of the three is now the second. Across all three schemes, 59 of 66 level readings stand apart against 41 before, and the mean margin rises 0.0132 to 0.0350. **The divergence fell further than the bar** — 0.3894 to 0.3555 against 0.4026 to 0.3905 — which is the check that separates a real gain from every candidate subject moving together. No arXiv or OpenAlex figure moved. **The leaders became right where they were wrong**: aeron and jPOS read `linguistics` on labels and now read `computer science`, fineract read `data mining` and now reads `computer science`, and CSO puts strata under `economics`, which is the only member it places outside computer science besides fix-trading-simulator. The two that still fail are both category-level and both inside a thousandth of a bit — fix-trading-simulator at 0.4250 against 0.4167 and maven at 0.4340 against 0.4327 — and fix-trading-simulator is one of the two members with under 1,200 declared runs, where a placement figure carries far more sampling noise. What is left: 61% of CSO's topics carry no link, so a subject reaches prose only through the children that do. Whether the labels should stay beside the prose or give way to it is unmeasured | nothing |
| | Whether arXiv goes | All three schemes place on every reading and arXiv and OpenAlex both stand apart from chance on the members. arXiv is too narrow for commercial software — 12 of its 174 rows are `q-fin` and `econ`. The works counts are the repair the OpenAlex placement still owes | [`PUBLISHED_QUANTITIES.md`](docs/plans/PUBLISHED_QUANTITIES.md) |
| | Carrying a quantity a publisher states about a concept | [`PUBLISHED_QUANTITIES.md`](docs/plans/PUBLISHED_QUANTITIES.md). `SkosConcept` is eight text columns, so OpenAlex's `works_count` and `cited_by_count` are dropped for all 4,516 topics — a 22,868-to-1 spread in breadth the placement cannot see. The plan chooses a sibling resource from the same extraction pass; which reading the counts feed is measured on the members before it is decided | nothing |
| | The vote itself | [`THE_VOTE.md`](docs/plans/THE_VOTE.md). One unit per resource, pooled where this library says pooled rather than summed, the constants last; `PhraseSpecificity` is computed on every sighting and weighs nothing | nothing |
| | The backtest | [`THE_PANEL.md`](docs/plans/THE_PANEL.md). The machinery, the manifest, the fetch and the Java scoring have landed. A new member arrives with a licence read at the pinned revision, a domain stated by somebody outside this project, and an expected result | nothing |
| | Classifying a repository against a taxonomy | [`CLASSIFYING_A_REPOSITORY.md`](docs/plans/CLASSIFYING_A_REPOSITORY.md). Chunk 1 landed. Chunk 2 names PyPI Trove `Topic ::` as the target — ACM CCS is refused because "educational and research purposes" is not redistribution — and the member manifest cites Apache DOAP category tokens recorded before the reading runs. Chunk 4 is the kill line | nothing |
| | What consumes the export | [`CONSUMING_THE_EXPORT.md`](docs/plans/CONSUMING_THE_EXPORT.md). The vocabulary pages consume `reading.json`; the other uses remain unbuilt | nothing |
| | The export owes five things | [`THE_EXPORT.md`](docs/plans/THE_EXPORT.md). The pull request arm is the largest; then the taxonomy revision, the declaration kind per word, permalinks, and the composition sitting in the test source set | the pull request arm needs a GitHub read and a token |
| | ~~How to call this as a library~~ | **Landed 2026-08-30 as `ONE_ANSWER.md` step 5.** `ExportedReading.of(Path, String)` reads a directory and returns the export, and the README states the call and the coordinate. `ExportFile` was already published and writes it. What is still test-only is `evidence.json`, which `EvidenceWorkings` assembles, so a consumer gets the answers and not the workings behind them | nothing |
| | Publishing to Maven Central | POM `url`, `developers` and `scm`, GPG signatures, and the Central Portal publisher API. The coordinate the README now states resolves from `~/.m2` after `publishToMavenLocal` and nowhere else | nothing |
| | The SKOS conversion is one flat package per publisher | One package per source under a shared core. The resources stay flat until `BundledVocabulary.files()` recurses, because a resource moved into a subdirectory would silently empty the list both provenance tests walk | nothing |
| | The extractors | [`THE_EXTRACTORS.md`](docs/plans/THE_EXTRACTORS.md). Six steps stated once instead of once per source. No further vocabulary lands until it does | nothing |
| | Industry vocabularies | [`INDUSTRY_VOCABULARIES.md`](docs/plans/INDUSTRY_VOCABULARIES.md). FIBO is bundled and unread on a finance member; a finance vocabulary must cover the ambiguous everyday words — `rate` reads as metrology, `leg` as anatomy, `strike` as sociology | nothing |
| | Technical taxonomies | [`TECHNICAL_TAXONOMIES.md`](docs/plans/TECHNICAL_TAXONOMIES.md). Seven domains software itself works in — the answer to arXiv holding no category for a payments system | licences, one per source |
| | Dependencies as domain | [`DEPENDENCIES_AS_DOMAIN.md`](docs/plans/DEPENDENCIES_AS_DOMAIN.md). A coordinate denotes one artefact and needs no disambiguation; `ImportOrigins` sorts them and nothing reads them as evidence | nothing |
| | The domain landscape of a large institution | [`DOMAIN_LANDSCAPE.md`](docs/plans/DOMAIN_LANDSCAPE.md). Which standards can be cited for a dozen business domains | licences — APQC sits behind a registration wall |
| | Prose trustworthiness | [`PROSE_TRUSTWORTHINESS.md`](docs/plans/PROSE_TRUSTWORTHINESS.md). `Weights.prose` at 0.5 is chosen where every other weight is derived, and it also gates the parse's string literals | nothing |
| | Topic leadership | [`TOPIC_LEADERSHIP.md`](docs/plans/TOPIC_LEADERSHIP.md). Most led files are led at a share under a fifth, and the correction is computed and unused | nothing |
| | The parse | [`THE_PARSE.md`](docs/plans/THE_PARSE.md). String literals and text blocks | the prose reading |
| | Supplied text | [`SUPPLIED_TEXT.md`](docs/plans/SUPPLIED_TEXT.md). A string or a file the reading is handed; then an issue reference and the credentials to read it | nothing |
| | Other languages | [`OTHER_LANGUAGES.md`](docs/plans/OTHER_LANGUAGES.md). SQL and TypeScript, one package per grammar | nothing |
| | What the author chose | [`WHAT_THE_AUTHOR_CHOSE.md`](docs/plans/WHAT_THE_AUTHOR_CHOSE.md). Thirteen items landed; the plan is only what is left | nothing |
| | The chance bar's cost, after the deal was made cheaper | Three changes landed 2026-08-30 and **none is measured yet**: `ScrambledTerms` spells each term once into a sort key rather than once per comparison, which was of the order of `2n log n` string allocations per deal over a 14,259-term vocabulary; `SpecificTerms.longestTerm()` reads a field taken at construction rather than a pass over every kept term on each call; and `ReachedPhrases` is one walk of the declared names answering both units, so a two-unit run deals and walks once. The pinned findings and `checkAll` are green, so no figure moved. **Measured 2026-08-31**: a read of this repository is 104.4 s with the export stage holding 71.6 s; a read of strata is 187.8 s with the export holding 79.6 s. The last figure recorded before the three changes was 123.8 s on this repository, at a different commit and a different corpus, so it is not a controlled comparison and no speedup is claimed — an A/B of one tree with and without them is what would attribute it. **What the two readings do establish is where the cost is.** strata writes nine times the declared runs and pays 1.1 times the export time, where every other stage scales with the repository. So the export is dominated by work that does not depend on the tree: `ScrambledTerms.of` permutes a vocabulary's own words at O(V log V) for CSO's 14,259 terms, 999 times per vocabulary, and never touches the declared names — the tree enters only at `.in(written)` afterwards. **The dealt indexes are therefore identical across every repository at one seed**, and `evaluationReadAll` rebuilds all 999 × 7 of them once per member. Building them once per JVM removes ten elevenths of that, and it is not the 2026-08-29 refutation, which filtered the repository's runs rather than the vocabulary's deals. **Two constraints the code imposes on how.** One `Random` is handed to each vocabulary in turn within a deal, so a vocabulary's deal depends on how many draws the vocabularies before it consumed — a cache keyed on the vocabulary alone would be wrong, and giving each its own derived seed would move every published figure. And holding all 999 deals of all seven vocabularies at once is 999 copies of a 14,259-term index, which is why they are built and discarded one at a time today. **The shape that satisfies both is to invert the loops**: deals outermost, members innermost, so one deal's seven indexes are built once and matched against every member before being discarded. That is a redesign of the member-parallel runner rather than a memoisation, and it is the next performance item. Left untouched deliberately: `TermSpans` allocates a list per candidate span at `spanOf`, and the words it folds were already folded once by `WrittenRuns`; and `PhraseStarts.couldBeIn` is O(names × length) on every deal, which is where the refuted 2026-08-29 optimisation aimed | nothing |
| | The arXiv chance field costs ~9.4 s of every read | Two options, deliberately left unattacked: memoise word-level lookups inside the draw loop, or precompute the chance field as a derived bundled resource keyed to the reader version | a decision between the two |
| | The dead-class half of the resource contract | [`LIMITS.md`](docs/plans/LIMITS.md). The class naming a resource must itself be named | nothing |

[`THE_LITERATURE.md`](docs/plans/THE_LITERATURE.md) is the survey of the field and ranks nothing here.

## Set aside

Plans that completed, were refuted by measurement, or were ended by a stated decision are deleted; `git log -- docs/plans/<name>.md` holds each document. The decision that ended the most of them at once: **the vocabulary is the one arm** — the concept and behaviour readings are recorded, exported and never repaired.

| Plan | What ended it |
|---|---|
| `A_VOTE_NOT_A_GATE` | The workings landed in `evidence.json`; the second-pass zero it also named belonged to the term path |
| `BEHAVIOURS`, `WHAT_IT_ACTUALLY_DOES` | The one-arm decision. No verbal taxonomy exists to match, and the behaviour arm left the export |
| `CLEANUP_AND_UPLIFT` | Discharged; the parked chance-field decision is a queue row |
| `EVERY_SIGNAL_REACHES_A_SUBJECT` | Step 1 landed; steps 2–4 asked the concept and behaviour arms to place, and the one-arm decision ended that |
| `JSON_ONLY_OUTPUT` | Complete: `reading.json`, `evidence.json` and `changes.json` are the output |
| `OLIA_DEFINITIONS` | The extraction landed; the pages that showed it went with the term path's reports |
| `ONE_ARM` | Its word-filtering half continues in `POOL_THE_CORPUS.md`; its condensation of the arms ended with the one-arm decision |
| `PHRASE_MATCHED_SUBJECTS`, `PHRASE_ARM_REPAIRS` | The arm was built and read below chance on Tika; repairs 0, 3 and 4 were measured and refused |
| `RANK_DO_NOT_GATE` | Steps 1–3 closed; continued from step 4 by `POOL_THE_CORPUS.md` |
| `RELEVANT_SUBJECTS` | The subtree walk's defects belonged to the term path |
| `SENSE_DISAMBIGUATION` | Superseded by `AGREED_SENSES.md` |
| `SUBJECT_CONDITIONED_MATCHING` | Conditioning the matcher ended with the term path; the scheme decision survives as a queue row |
| `TERM_MATCHING` | Depth was measured and refuted; the term path is being removed, and queue item 1 records its workings first |
| `THE_PIPELINE` | The ported project's pipeline; this library reads a clone path |
| `THE_REPORTS` | There are no reports |
| `UNREAD_SIGNALS` | All five ranked items landed, each with its measurement |
| `VISUALISATION` | Nothing is drawn; a chart, if one returns, starts from `reading.json` |
| `WIKTIONARY_HIERARCHY` | The fold landed; the rest needs a published mapping between two label spaces, and none exists |
