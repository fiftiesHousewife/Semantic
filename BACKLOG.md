# Backlog

One ranked list of everything open, and an index of the plans under [`docs/plans/`](docs/plans) that carry the detail. Each plan states what it would measure, what would settle it, and what would end it; nothing is argued here.

Those documents are outside the corpus this library reads — [`.readingignore`](.readingignore) states so, and `StatedExclusions` is what honours it. A specification written in the vocabulary of a domain would put `ForeignExchange` and `InterestRate` into the tree and then find them.

New to the terms? [`docs/GLOSSARY.md`](docs/GLOSSARY.md) defines every linguistic and statistical word used here, with a reference for each. Library choices and their licences are in [`docs/DEPENDENCIES.md`](docs/DEPENDENCIES.md).

**State on 2026-09-06, at reading schema 26.0 and evidence 6.0.** Four rows closed on 6 September — the dead `TaxonomyShape`, the twice-written resource header, the twice-walked publisher tree, and row 47: `code-semantics-api` is dissolved at `ded6bd0`, its dead contracts deleted and its survivors folded into the engine — and row 20's steps 3 and 4 landed: `taxonomies[].bar` publishes `fieldMembers` beside `field`, so a moved bar is read against a changed membership; `ProvidedTermIndexes` finds a consumer's own `TermIndex` on the classpath and the default export judges it in the same field, so the row stays open on the surveyed candidates alone, and `fetch-candidate-vocabularies.sh` fetches their sources with pins and digests recorded. `RenderedResourceTest` re-renders all eleven taxonomies from their own read-back and found the arm-to-path rename stale in the committed FIBO header, now fixed. The remaining rows are live, and the figures in row 1 moved and are restated. `cleanTest checkAll` is green over all nine modules' checks, the `pinned` findings are green, and `./gradlew read` is green at schema 26.0, with the self reading committed at `d653ad7`. The eleven evaluation members are reread at 26.0 — tika's regenerated reading is identical to the committed one — and the pages have not been rebuilt since `100c946`; no page output is tracked.

## What the codebase does, step by step

The library takes a local directory and produces a distribution over subject categories, plus a statement of which parts of the repository depart from that distribution further than chance would. Nine steps run in order. Each is named here with the classes that carry it, so a plan below can say which step it changes.

**1. The files are found and parsed.** `JavaSourceScopes` finds every `<module>/src/<set>/java` directory and `DocumentationScope` finds the markdown, both filtered by whatever `.readingignore` states through `StatedExclusions`. `ParsedRepository` parses each Java file with JavaParser and keeps only the names its author *declared* — a type, method, field, parameter, local, record component, constant, pattern binding or label. A name that is used here but declared elsewhere is somebody else's vocabulary, and the parse is the only thing that tells the two apart. `ImportOrigins` sorts each import into platform, own and third-party by asking `PlatformPackages`, which cites `ModuleFinder.ofSystem()`. Files that do not parse are counted and named.

**2. Each name is split into words.** `Tokeniser` splits on case transitions and separators; `IdentifierWords` applies it to a declared name and adds the acronym-run rule. A letter beside a digit is not a boundary, which is [UAX #29](https://www.unicode.org/reports/tr29/)'s own rule WB9/WB10 and not a gap. Where a compound carries no boundary at all, `WordSegmenter` enumerates candidate splits through `CompoundParses` and prices each piece with `PieceCost` against the frequency list `WordRanks` reads, so the commonest reading wins. This is grammar and it is permitted; a list of words to treat specially would not be. `CollocatedWords` then reads the words back in the order they were written: a run of adjacent words a topical resource publishes as one entry — `PublishedPhrases` holds the 69,713 the two bundled ones state — is one word from here on, and a run must begin and end on a word that carries subject matter, because the same dictionaries publish `to the` and `out of`.

**3. Each word is looked up.** `ContentWords` asks WordNet whether the word has an open-class entry, which removes the words English uses to hold a sentence together without a stop list existing. `WordMorphology` returns the dictionary form from the same query. `TopicCitations` then collects what two resources say the word is about: WordNet Domains labels each *sense*, and Wiktionary's topic vocabulary labels the *headword*. `StatedTopics` folds a label that another label of the same word already implies back into the label it came from, using the hierarchy the topic resource itself publishes.

**4. Each vote is weighted.** `SenseCoverage` gives the share of a word's senses the resource labelled. `WordSpecificity` gives `log(rank) / log(size)` against the frequency list, bounded in `[0, 1]` by the length of that list. `PhraseTopics` scores a subject by the geometric mean over the words of the name that agree on it, times the share of the name that agrees. Every weight is read off a published resource; none is chosen.

**5. The votes are pooled.** `TopicDistribution` accumulates one unit of evidence per phrase — one declared name, or one sentence of prose at half the weight — into a distribution over subjects for each scope and for the repository as a whole, over everything observed rather than over what was placed: what a phrase's words could not settle on a subject stays in the denominator, so a share says how much of what was written it is and not how much of what happened to resolve. `PooledLogOdds` and `AxisEvidenceAccumulator` do the arithmetic for a closed set of values, `OpenSpaceAccumulator` for an open one. `LegibilityReading` reports λ, the share of word occurrences any resource could be cited for, which is the denominator the rest rests on.

**6. Each scope is compared against the whole.** `JensenShannon` measures the distance between a scope's distribution and the repository's, in bits, with a per-topic decomposition that says which topics carried the distance. `PermutationNull` draws 999 resamples of a scope of the same size from the same repository, and `ChanceExpectedBest` sets the bar at the `1/(n+1)` quantile because every scope is tested at once. `Thresholds` holds the constants.

**7. The repository is placed in a published field.** `ArxivSubjects` supplies 152 subject descriptions, `PooledDescriptions` pools each subject's own prose through the same pipeline, and `SubjectPlacement` reports the nearest by divergence. Three schemes are placed against and none is picked: arXiv's categories, OpenAlex's topics, and CSO's, whose subjects `CsoSubjects` states by the labels of the topics beneath them because CSO publishes no prose. `SubjectNull` builds a taxonomy of chance and the nearest real subject must beat the nearest chance one. `OrdinaryEnglish` provides the same reading over the frequency list, as a baseline.

**8. Declared names are matched against a published term vocabulary.** `TermIndex` holds a taxonomy's concepts, already formatted as identifiers; `TermSpans` takes the longest term at each position of a name, left to right, with no two matches overlapping; `NormalisedTerms` reduces both sides to words, to dictionary forms, or to synsets, and reports each level separately. `TaxonomyTree` and `StatedSiblings` supply the publisher's own placement, and `CorroboratedTerms` accepts a single-word match only where the repository writes another concept from the same branch. `ScrambledTerms` deals each publisher's own words across its own terms 999 times and `TermOrderNull` sets the bar at what those deals reach, so a vocabulary is published only where it beats a permutation of itself; `MatchedPhrases` counts terms of more than one word and `PhraseOccurrences` counts how often they are written. **Nothing from this step feeds step 5.** It runs against this repository and against the eleven evaluation-set members.

**9. The reading is written, as JSON and nothing else.** `EvidenceKinds` takes the answer from the most specific evidence standing above chance and `summary.answers` carries every source that cleared that level's bar; `StatedDescriptions` gives each matched concept the nearest description its publisher states, walking `StatedAncestry.pathAbove` where the concept itself has none. `ReadingSummary` keeps what cleared a bar and names what did not, and `ExportedReading` writes [`output/json/reading.json`](output/json/reading.json) — the answers, validated against `reading-export.schema.json` before the file exists. `ThemeGraph.evidence()` writes `evidence.json` beside it, which is the workings and which nothing validates. `ChangeFile` writes `changes.json` where the previous reading shares a `schemaVersion`, stating each figure before and after and naming every topic, word and concept that entered or left. `PublishedFormat` refuses any name that is not `.json`. All three are committed, so the current figures can be read without running anything.


## The list

Everything open, ranked. The first seven are defects in what the library already publishes and are ordered; below them the ranking is a judgement rather than a schedule. A row names its plan document where one exists, and the plan holds the measurements.

| | Item | What is left, and what would settle it | Plan | Blocked on |
|--:|---|---|---|---|
| 1 | The tree is over two of its own stated limits | 20 classes under `src/main` exceed the 150-line limit — `DomainOverlap` 219, `ParsedRepository` 213, `TaxonomyTree` 203, `ThemeReading` 199, `TaxonomyMatchesPage` 198, `DrawnReading` 194 — and no line exceeds 120 characters. `ExportedReading`, `DrawnReadings`, and on 6 September `WordNetLexicon`, `TermTree`, `PhraseTopics`, then `JavaSource` (into `DeclaredOccurrences` and `DeclaredNameForms`) and `EvidenceKinds` (into `AnswerFromATaxonomy`, `AnswerFromAScheme` and `TermLength`) were split by responsibility; each remaining class is the same split, not a reformat | | nothing |
| 2 | A concept the publisher places nowhere and files in nothing groups under nothing | **The premise this row carried — that the level a reader wants sits in the middle of the path — is refuted.** `QuantitativeValue` holds 2 of FIBO's 1,827 concepts, so it is not a broad level: strata's 1,431 occurrences under it are one concept, `PresentValue`, written 1,429 times, and FIBO states `QuantitativeValue` as its only parent, so no rule deriving a depth reaches a better one. The row's own example says the same — `FixedLeg` groups under `SwapLeg`, which holds 22 concepts, where the `DerivativeTerms` it proposed holds 33. **The defect was the blank branch**, and `StatedPaths` closed most of it on 2026-09-02: where a publisher states no level above a concept, `statedPath` now names the module it files that concept in — FIBO's domain, FpML's schema file group, BIAN's business area — refusing a module an outright majority is filed under by the bound `StatedAncestry.fieldLevels` already states. It is read only where the chain states nothing, because a stated parent is the better answer and reading both put an ontology file name over six concepts on OLiA's discourse relations. Over the twelve readings the occurrences under no branch fall **4,126 to 261** and no other grouping moves, because FIBO files 71% of its chains in FND and OLiA 82% of its in olia-top.owl and both are refused. **What is left is the 261**: 248 are FIBO rows taken from the OMG Commons Ontology Library, whose URIs name a module — `.../Commons/Organizations/LegalEntity` — that `FiboConcepts.moduleOf` reads only out of a `/fibo/ontology/` URI, and 13 are FIX rows placed under `Common` and filed in nothing, which is all FIX states. **Settled by**: reading the Commons segment, which needs the extraction re-run against a fetched copy | | a fetched FIBO copy |
| 3 | Termhood for a single word | `MatchedPhrases` admits only terms of two or more words, justified on unithood grounds, and nothing here has ever asked whether a single word is a term of art. Both ingredients exist and are cited — `WordSpecificity` and WordNet's sense count — and `ScrambledTerms` already deals one-word shapes, so the existing null generalises without change. The worked case in this repository's own reading: `collocation`, published by OLiA, written 35 times, specificity 1.000, refused for being one word. Kageura and Umino 1996 is the published grounding | | nothing |
| 4 | The unit the bar counts in, and the three verdicts that move with the seed | Measured on all eleven members, 77 verdicts, one seed in 1,497 s and three in 4,622 s. Counting terms alone gives 26 clears, counting occurrences alone 22, requiring both 17; the two units disagree about 14 verdicts and **each admits false positives the other refuses** — terms alone admits tika clearing CWE on `process control` 4 and `infinite loop` 2, occurrences alone admits FpML clearing aeron at 17.5× where `correlation_id` supplies 850 of 857. **74 of 77 verdicts are seed-independent**; the three that move are strata/BIAN, quickfixj/FpML and besu/FIX, each marginal in one unit and steady in the other. `HOW_MANY` is what the reading publishes and the conjunction is a candidate the evidence supports, not a rule the reading applies. **Settled by**: the same eleven at 10⁴ resamples, which is what a non-zero variance asks for. `./gradlew phraseUnitAll -Dcs.evaluation.dir=$HOME/evaluation`, and read the terms with `./gradlew termhood` beside the bars | | nothing |
| 5 | Three questions the description repair left | The repair landed at schema 24.0 and reproduced its own survey within one concept — 556 described at the node against a predicted 555, 229 by walking up, 61 undescribed against 62, over 846 distinct concepts. Open: **whether a description taken two or more steps up is still about the concept**, settled by reading the 105 CSO concepts that walk two or more steps and counting how many name a subject the repository does not write; **whether an expansion and a definition can be told apart without naming vocabularies**, settled by asking WordNet whether the first noun of the definition is a hypernym of the label; and **whether the 45 undescribed concepts share a cause** — they are `session types`, `revenue`, `BusinessCenter`, `ProductType`, `SpecifiedCurrency` and their kin | | nothing |
| 6 | `WordMorphology`'s prefix list has no citation | The class is live on the hot path — `WordMorphology` → `WordSegmenter` → `IdentifierWords` → every reading — and carries three lists. The suffix list and the vowel-initial list are arguably grammar, a near-closed inflectional class. **`PREFIXES` is not**: un, re, de, non, pre, dis, mis, over, under, sub, anti, semi — twelve chosen items with nothing stating why twelve, no derivable bound, and no `inter-`, `multi-`, `trans-` or `co-`, which code writes. The class javadoc claims all of it is grammar. **The citable replacement for the suffix half is already a dependency**: WordNet's Morphy publishes its own detachment rules and exception list through extjwnl's `MorphologicalProcessor`, which is the move `PlatformPackages` and `ContentWords` already make. Prefixes have no such citation, so the honest step is to delete `carriesAPrefix` and let `changes.json` say what it cost | | nothing |
| 7 | `ContentWords` refuses a word shorter than three letters | The rule removes `id` for the wrong reason — `systemId` and its kin are the platform's spelling — and length is a stated bound where a derived one is required | | nothing |
| 8 | The pages take the shape of the answer | Steps 1a to 1f landed on 2026-08-31: `readings.html` is the root, the figure draws one mark per answering source on a log scale with the bar as a dashed rule, `PagesIndex` and `index.html` are deleted, and `evaluationPages` reads the manifest itself. **Steps 2 to 5 are open**: `reading.html` per reading, the bar and the two `setAside` counts reaching a page, one `pages` task replacing `TaxonomyMatchesCommand`, `TermTreesCommand`, `DomainVennCommand` and `VocabularyPageCommand`, and the three printing probes moving out of `vocabulary-page`. Two orphans sit in the untracked `build/` directory with no writer left in the source: `index.html` and every `control-matches.html` | [`ONE_PAGE.md`](docs/plans/ONE_PAGE.md) | nothing |
| 9 | A readable name for every subject | Five of the twelve readings name their subject with an identifier — `Session`, `SingleGeneralOrderHandling`, `QuantitativeValue`, `StartingDate`, `InflectionTypeFeature`. The plan takes the name from the publisher's own file or from the identifier splitter's grammar, never from a name written here, and it is measured: **FIX documents 7,003 of its 7,170 rows and 2 of its 39 categories**, so `MsgSeqNum` states *"Integer message sequence number."* and `Session` states nothing. Four steps, none started. It overlaps the landed description repair at step 2 and needs re-reading against it | [`HUMAN_LABELS.md`](docs/plans/HUMAN_LABELS.md) | nothing |
| 10 | The words that agreed per phrase | `evidence.json` now carries most of what this row asked for: `workings.words` states each word's occurrences, sense coverage, specificity and every topic it contributed to with the resource that stated it; `workings.removed` names each dropped topic with the rule that dropped it; `workings.unread` records a run set aside with its reason; and `matches` carries the level, the specificity and the sites. **What is still missing is the phrase**: `PhraseTopics` scores a subject by the geometric mean over the words of a name that agree, and no file says which words agreed on which phrase. The `debugging-a-reading` skill holds the method meanwhile | | nothing |
| 11 | The corpus owes three derived bounds and one grammar rule | Steps 1 to 5 are done and the reference is a hundred repositories, 28,839 words over 1,541,720 occurrences. **The tail**: truncating promotes exactly the words cut, because a word absent from a reference has a share of zero, so the bound must come from leave-one-out — pool ninety-nine, read the hundredth, and find the count below which a word's share moves more between draws than between corpora. **Whether a hundred is enough**: the same leave-one-out answers it, and a seeded draw extends without being re-rolled. **The size floor**: one megabyte is stated, not derived, and is the one number in the frame somebody could argue with. **The letter–digit boundary**: 6,932 of the thirty-repository reference's 14,763 rows carried a digit, and splitting `utf8` from `col1` needs the cited-token catalogue `CitedTokens.NONE` stands in for | [`POOL_THE_CORPUS.md`](docs/plans/POOL_THE_CORPUS.md) | the catalogue, for the last of the four |
| 12 | A sense weighed by what is written beside it | `DomainOverlap.guidedByTheDiscourse` runs the two-pass division on the pages; the reading itself still weighs every sense by its tagged count alone, so `read` gives `publishing` its whole counted weight in a library that never means it. Gale, Church and Yarowsky's one sense per discourse and Yarowsky's confidence floor are the published grounding. Abandon if the evaluation members' rankings fall | [`AGREED_SENSES.md`](docs/plans/AGREED_SENSES.md) | nothing |
| 13 | eXtended WordNet Domains keeps one label per synset, and that is the defect | Weight-scaled label voting landed at `0e0226d` and is **inert on the nine**: every leading domain unchanged, top-3 concentration moving at most 0.8 points, unlabelled going 0% to 4–8%. It cannot win, because the resource's strengths compress toward 1 — median 0.94, only 5% below 0.69 — and the motivating defects are wrong labels in the one-domain-per-synset reduction, which no per-label discount reaches: aeron's geometry lead through `length`, maven's archaeology through `artifact`, `lemma` on philosophy. **Settled by**: keeping the top-k of the propagation vectors so a close race carries both labels, or correcting the reduction | | nothing |
| 14 | The read's remaining cost is building the deals | Three memoisations landed 2026-08-30 and took a read 44% faster with no figure moved. **Measured 2026-08-31**: this repository 104.4 s with the export stage holding 71.6 s; strata 187.8 s with the export holding 79.6 s. strata writes nine times the declared runs and pays 1.1 times the export time, so the export is dominated by work that does not depend on the tree — `ScrambledTerms.of` permutes a vocabulary's own words at O(V log V) for CSO's 14,259 terms, 999 times per vocabulary, and `evaluationReadAll` rebuilds all 999 × 7 indexes once per member. **The dealt indexes are identical across every repository at one seed.** Two constraints on the repair: one `Random` is handed to each vocabulary in turn, so a cache keyed on the vocabulary alone is wrong and a derived per-vocabulary seed moves every published figure; and holding 999 deals of seven vocabularies at once is 999 copies of a 14,259-term index. **The shape that satisfies both is to invert the loops** — deals outermost, members innermost — which is a redesign of the member-parallel runner. Beside it: the arXiv chance field costs ~9.4 s of every read and has two options, memoising word-level lookups inside the draw loop or precomputing the field as a bundled resource keyed to the reader version; and `PublishedPhrases.runOf` holds 6.3% of the profile spelling a candidate key with `String.join` and discarding it. **Do not retry**: raising the extjwnl dictionary cache capacity, measured 2026-08-18 and reverted | | the arXiv choice is a decision between two options |
| 15 | Enrichment: three candidates, each of which moves every bar | Surveyed 2026-08-31 and ranked twice, and the two rankings invert. **By phrases added**: BIAN's ~20,000 DDD entity names lead. **By what a reader can read**: BIAN's `ATMNetworkOperatingSessionServiceProviderReference` describes nothing, and FIX's 4,445 code names lead — readable and each carrying an `xsd:documentation` synopsis: `StopLimit`, `MarketOnClose`, `GoodTillCancel`. FpML's 358 types in the five unread views sit between at about 20 words of documentation each, and the bundled header's claim that the other views are subsets of the confirmation view is refuted. **There is no display-only path**: a description is downstream of matching or placement, so every candidate enters through one of them and moves all seven bars on all twelve readings. FIX's code names need a derived length bound, since 80.7% are multi-word | [`EXTENDING_THE_TAXONOMIES.md`](docs/plans/EXTENDING_THE_TAXONOMIES.md) | the choice of which, and the field's membership |
| 16 | BIAN's own ISO 20022 annotations, as a second citable layer | `bian-official/public` at `release14.0.0/apis-iso20022_ext-ddd/` is Apache-2.0 and states, inside BIAN's own files, an ISO 20022 business-component type per API attribute across 259 service domains. The ISO type names are quoted by a publisher this project already extracts, so reading them costs ISO nothing and needs no ISO licence. It would give BIAN a vocabulary of type names attached to named service domains where today it has 319 capability labels, which is why it reaches six spans on a payments library. **It is a different repository from the pinned `artefacts`**, so it needs its own `ContentDigest` pin and its licence read at that revision. **Settled by**: extracting the type names, matching them, and reading whether jpos and fineract gain phrases while the eight members outside banking do not | | nothing |
| 17 | The module codes get the names their publishers state | The trees and phrase areas group under module codes — FIBO's FND, FpML's shared and msg. Measured 2026-08-27: FIBO's pinned revision states a name per domain, and `MetadataFND.rdf` labels FND *foundations domain*, so the FIBO extraction can carry names instead of codes. FpML's pinned schemas state no name for a file group, so its codes are the publisher's whole statement and a name needs a new pinned source. FIX fields are no longer flat — `FixFieldPlacements` reads the `fieldRef`s of the messages, components and groups that name a field for 5,378 of the 6,203 | | a pinned source, for FpML |
| 18 | FIBO named its parents by fragment, and two ontologies can spell one the same | The dropped-heading half is fixed: the extraction now carries the superclass FIBO states whichever ontology publishes it, and blanks fell from 274 to 46 at the pinned revision 119fa8c0. **What is left is the collision.** `broader` names a parent by its URI fragment, and **63 of the 291 out-of-namespace parents name a fragment FIBO itself also publishes** — Commons' `LegalEntity` above `Corporation`, Commons' `Organization` above `Syndicate`. Those 63 were never blank: the old rule matched the name and placed them under FIBO's concept of that name, which is usually but not provably the same concept, and the fix inherits that. **Settled by**: whether the pairs are the same concept, which the two ontologies' own definitions answer where both state one, and whether `broader` should hold the identifier rather than the fragment for every source — a change that moves every taxonomy's tree and needs its own reading | | nothing |
| 19 | CSO's prose, and whether its labels stay beside it | `CsoSubjects` states a subject as its labels **and** the Wikipedia summaries its own `owl:sameAs` names — 46,477 words across the 225 placement subjects against 5,828 on labels alone — and that took CSO from 2 of 22 level readings standing apart to 20 of 22, with arXiv at 18 and OpenAlex at 21. **What is left**: 61% of CSO's topics carry no link, so a subject reaches prose only through the children that do, and whether the labels should stay beside the prose or give way to it is unmeasured. **The unread half of the same link**: CSO states 19,297 `sameAs` triples over 5,294 topics pointing at DBpedia, Wikidata, YAGO and Freebase, and Wikidata's descriptions are CC0 with labels resolving from the entity ids CSO already states. `wikidata.org` is not on the session network allowlist, so the descriptions have to be fetched outside the sandbox and passed in with `-P`, as every other extraction takes its source | | a Wikidata fetch, for the second half |
| 20 | The pattern for extending to further taxonomies, and the fields for it | **Steps 1, 2a and 2b landed 4 September 2026.** Every one of the 24 bundled resources states `# Name:`, `# Publisher:` and `# Kind:`; the 11 that are taxonomies of concepts also state `# Short name:`, `# Published at:`, `# Description:` and `# Subject:`, and `StatedProvenance` reads all seven. `BundledTaxonomies` replaced `MatchedTaxonomies` and `ControlTaxonomies`, and eleven constants left seven classes. **`Kind: terms | subjects` covers 11 of the 24** — the other 13 are not taxonomies of concepts, so the closed set is five: `terms` 6, `subjects` 5, `words` 8, `identifiers` 2, `frequencies` 3. **Two findings the plan did not have**: there were three enumerations and not two, because `TermVocabularies` sits in `lexicon` below the module a `TermIndex` lives in and stays; and the matched list is not derivable from a header, because BIAN carries prose for all 319 rows and is matched anyway. **Step 3 landed 6 September at schema 26.0**: `PhraseBar` carries the membership and derives the quantile from its size, and `taxonomies[].bar` publishes `fieldMembers` beside `field`, so a bar moved by an added vocabulary is read against a changed set. **`ServiceLoader<TermIndex>` landed 6 September**: `ProvidedTermIndexes` orders the classpath's implementations by their sources' own names, `ExportedReading.vocabulariesBesidesOlia()` is the one statement of the default field with the provided ones in it, and `JudgedTaxonomies` refuses a field where two vocabularies state one source name. **The first two candidates are measured, 6 September, on all eleven members** — the plan's own table holds the figures. PRONOM: 2,571 formats extracted from the DROID signature file V125; clears its permutation bar only on tika, 20 phrases against 6, and stays below on the other ten — the first term vocabulary to pass the out-of-domain control FIX and FpML failed; tika's `xhtml` 352 moves out of the unread tail and `config` 503 does not, because the registry never states the token. execution-specs: 824 declarations of the fork the spec schedules last; besu writes 163 phrases against 26 — a wider separation than any bundled vocabulary reaches — and `blockchain` 624 is stated where `frame` 791 and `tx` 304 are not, but the raw labels also clear on seven of ten out-of-domain members because the spec writes its state helpers in general words (`account`, `balance`, `less_than`), so it is **refused, in both variants**: the stated second measurement dropped the 212 module-level functions and besu widened to 6.6× while fineract, jpos, strata and aeron still cleared — the residue is `transaction type` written 397 times in fineract and its kin, labels the spec genuinely states that any transaction system writes, so no structural rule closes the gap. **The XML security candidates are measured, 6 September, on all eleven members, and both are refused as extracted.** CSRC: 9,140 terms from the pinned glossary export; santuario writes 63 phrases against 11 — `signature verification` 31, `xml signature` 29 — but the glossary defines everything NIST's publications define, including a blockchain vocabulary, so ten of eleven members clear and the termhood narrowing widens besu to 15 against 1 (`time stamp` 409, `access list` 282, `block header` 273 are all NIST's own labels) rather than ending the leak; `cert` 269 and `sig` 227 stay in santuario's unread tail because the glossary states each only as a single word. JOSE: 140 registered values from the nine IANA registries; santuario writes 15 phrases against 3, but the descriptions are the English any key-handling program writes (`key value`, `key pair`, `content type`) and seven controls clear on them, while the narrowing silences santuario itself. What remains of the row is the PRONOM promotion decision, with EDAM waiting on it | [`EXTENDING_THE_TAXONOMIES.md`](docs/plans/EXTENDING_THE_TAXONOMIES.md) | nothing |
| 21 | A linguistics vocabulary beyond OLiA | Candidates measured 2026-08-27: LexInfo 3.0 (259 classes, CC BY 4.0 stated in the file, Apache-2.0 repository) with its OntoLex core (CC0 stated in the file) is the one clean ecosystem — noun phrase, collocation, morphological root, lexical concept, concept set, canonical form. GOLD is out at the licence gate, and the NLP fields taxonomy (BSD-3, 83 fields, prose per concept) is placement-shaped rather than matched. **None states the measured gap words** — lemma, lemmatisation, head word, content word, open class — so the bundle raises coverage without closing the gap. Bundle LexInfo and OntoLex per the `adding-a-taxonomy` skill when a linguistics member joins the evaluation set, and measure then | | a linguistics evaluation member |
| 22 | Whether arXiv goes, and the quantities a publisher states | All three schemes place on every reading, and arXiv and OpenAlex both stand apart from chance on the members. arXiv is too narrow for commercial software: 12 of its 174 rows are `q-fin` and `econ`. The repair the OpenAlex placement owes first is the works counts — `SkosConcept` is eight text columns, so `works_count` and `cited_by_count` are dropped for all 4,516 topics, a 22,868-to-1 spread in breadth the placement cannot see. The plan chooses a sibling resource from the same extraction pass, and which reading the counts feed is measured on the members before it is decided | [`PUBLISHED_QUANTITIES.md`](docs/plans/PUBLISHED_QUANTITIES.md) | nothing |
| 23 | The `law` floor survives the coverage discount | `TopicTally` scales every phrase by credence and the licence vocabulary still floors the theme. The finding stands and the repair is unfound | | nothing |
| 24 | A topic vocabulary's own labels do not label themselves | `cryptography` is a topic the bundled vocabulary states, and the word `cryptography` carries no topic: 470 of 719 labels | | nothing |
| 25 | `PooledDescriptions` joins one subject's last word to the next subject's first | The pooled level joins descriptions with a single space, and `CollocatedWords` reads a pair straddling the join as a phrase neither publisher wrote. Unmeasured: how many collocations the joins create across the pooled levels | | nothing |
| 26 | The summary names a word and its plural as two findings | `word` 395 and `words` 178 both stand in the export's ten leading words, and so do `concept` 165 and `concepts` 99. **Settled by**: how much of the ranking's divergence the two spellings hold together against either alone, on the evaluation members | | nothing |
| 27 | The placement reads a linguistics library and a text-extraction toolkit as one category | Under both schemes tika and this repository read the same category — `cs.CL` and Natural Language Processing Techniques — while maven separates from both. The twelve committed readings under [`output/`](output) are the evidence for whether that is a fault of the scheme or a fair reading | | nothing |
| 28 | The three seams the doctrine holds open | Stated in [`CLAUDE.md`](CLAUDE.md) and repeated here so they are ranked with everything else. **`CitedTokens.NONE`** is the default the word segmenter runs on, so a compound is read by word frequency alone; the bundled Wikidata initialism registry cannot fill it, and `CitedTokenCatalogueTest` measures why — 74,397 tokens, 14,322 of three letters, and 1,195 among the ten thousand commonest English words, including `CODE`, `DATA`, `NAME`, `TYPE`, `LIST`, `NODE`, `SIZE`. Narrowing it by the prominence the registry itself states is a bound and must be derived on repositories this reading was not written for. **`Tokeniser`** is the ported schema-identifier grammar and is too narrow for code: `XMLHttpRequest`, `parseHTTPResponse`, `toJSONString`, `getDSLContext`, `IPv6Address`, each pinned by `TokeniserTest`. What is left of it is the letter/digit boundary, blocked on the catalogue, and byte offsets on a token. **`AxisEvidenceAccumulator`** partitions over a closed enum, and concepts are an open space of about 10⁵ synsets needing a sibling accumulator whose partition runs over the values evidence was recorded for plus one explicit mass for what did not resolve | [`LIMITS.md`](docs/plans/LIMITS.md) | the catalogue, for the first two |
| 29 | The vote itself | One unit per resource, pooled where this library says pooled rather than summed, the constants last. `PhraseSpecificity` is computed on every sighting and published on every match row, and no weight reads it | [`THE_VOTE.md`](docs/plans/THE_VOTE.md) | nothing |
| 30 | The backtest | The machinery, the manifest, the fetch and the Java scoring have landed, and eleven members are cloned at `$HOME/evaluation`. A new member arrives with a licence read at the pinned revision, a domain stated by somebody outside this project, and an expected result. The document is [`THE_EVALUATION_SET.md`](docs/plans/THE_EVALUATION_SET.md), renamed with the term. **The set still has no FpML-consuming member**: `handcoded/fpml-toolkit-java` reads now rather than throwing, and it reads empty — 195 Java files under `src-core`, `src-fpml` and `src-dsig`, `javaFilesNoScopeReached` 195, signals, themes and concepts all zero, and arXiv and OpenAlex placing it on its README alone. The count says why the reading is empty; it does not fill it, so the member is still refused | [`THE_EVALUATION_SET.md`](docs/plans/THE_EVALUATION_SET.md) | nothing |
| 31 | Classifying a repository against a taxonomy | Chunk 1 landed at Spearman rho 0.9985. Chunk 2 names PyPI Trove `Topic ::` as the target — Apache-2.0, 321 classifiers, 25 roots — because ACM CCS grants "educational and research purposes", which is not redistribution, and the member manifest cites Apache DOAP category tokens recorded before the reading runs. Chunk 4 is the kill line | [`CLASSIFYING_A_REPOSITORY.md`](docs/plans/CLASSIFYING_A_REPOSITORY.md) | nothing |
| 32 | The export owes five things | The pull request path is the largest; then the taxonomy revision, the declaration kind per word, permalinks, and the composition sitting in the test source set | [`THE_EXPORT.md`](docs/plans/THE_EXPORT.md) | the pull request path needs a GitHub read and a token |
| 33 | What consumes the export | The vocabulary pages consume `reading.json`; the other uses remain unbuilt. `evidence.json` is assembled by `EvidenceWorkings` in the test source set, so a consumer of the published library gets the answers and not the workings behind them | [`CONSUMING_THE_EXPORT.md`](docs/plans/CONSUMING_THE_EXPORT.md) | nothing |
| 34 | Publishing to Maven Central | POM `url`, `developers` and `scm`, GPG signatures, and the Central Portal publisher API. The coordinate the README states resolves from `~/.m2` after `publishToMavenLocal` and nowhere else | | nothing |
| 35 | The SKOS conversion is one flat package per publisher | One package per source under a shared core. The resources stay flat until `BundledVocabulary.files()` recurses, because a resource moved into a subdirectory would empty the list both provenance tests walk, and neither test would report it | | nothing |
| 36 | Industry vocabularies | FIBO is bundled and a finance vocabulary must cover the ambiguous everyday words: `rate` reads as metrology, `leg` as anatomy, `strike` as sociology, and the jargon is 82 occurrences | [`INDUSTRY_VOCABULARIES.md`](docs/plans/INDUSTRY_VOCABULARIES.md) | nothing |
| 37 | Technical taxonomies | Seven domains software itself works in — the answer to arXiv holding no category for a payments system | [`TECHNICAL_TAXONOMIES.md`](docs/plans/TECHNICAL_TAXONOMIES.md) | licences, one per source |
| 38 | Dependencies as domain | A coordinate denotes one artefact and needs no disambiguation; `ImportOrigins` sorts them and nothing reads them as evidence | [`DEPENDENCIES_AS_DOMAIN.md`](docs/plans/DEPENDENCIES_AS_DOMAIN.md) | nothing |
| 39 | The domain landscape of a large institution | Which standards can be cited for a dozen business domains | [`DOMAIN_LANDSCAPE.md`](docs/plans/DOMAIN_LANDSCAPE.md) | licences — APQC sits behind a registration wall |
| 40 | Prose trustworthiness | `Weights.prose` at 0.5 is chosen where every other weight is derived, and it also gates the parse's string literals | [`PROSE_TRUSTWORTHINESS.md`](docs/plans/PROSE_TRUSTWORTHINESS.md) | nothing |
| 41 | Topic leadership | Most led files are led at a share under a fifth, and the correction is computed and unused | [`TOPIC_LEADERSHIP.md`](docs/plans/TOPIC_LEADERSHIP.md) | nothing |
| 42 | The parse | String literals and text blocks | [`THE_PARSE.md`](docs/plans/THE_PARSE.md) | the prose reading |
| 43 | Supplied text | A string or a file the reading is handed; then an issue reference and the credentials to read it | [`SUPPLIED_TEXT.md`](docs/plans/SUPPLIED_TEXT.md) | nothing |
| 44 | Other languages | SQL and TypeScript, one package per grammar | [`OTHER_LANGUAGES.md`](docs/plans/OTHER_LANGUAGES.md) | nothing |
| 45 | What the author chose | Thirteen items landed; the plan is only what is left, and it holds the verb objects from declared parameter and return types that the phrase work left outstanding | [`WHAT_THE_AUTHOR_CHOSE.md`](docs/plans/WHAT_THE_AUTHOR_CHOSE.md) | nothing |
| 46 | The dead-class half of the resource contract | The class naming a resource must itself be named. `VocabularyProvenanceTest` enforces that every bundled resource carries a provenance header and `BundledResourceReachabilityTest` that nothing bundled goes unread; neither says a class nobody calls has stopped counting | [`LIMITS.md`](docs/plans/LIMITS.md) | nothing |

[`THE_LITERATURE.md`](docs/plans/THE_LITERATURE.md) is the survey of the field, [`A_CORPUS_NOT_AN_INDEX.md`](docs/plans/A_CORPUS_NOT_AN_INDEX.md) the reasoning the reference corpus rests on, and [`docs/reference/`](docs/reference) the surveys behind both. None of them ranks anything here.

## Closed, and not to be re-proposed

Items that landed, were refuted by their own measurement, or were ended by a stated decision. They are off the list above; each line says what closed it so it is not raised again.

| Item | What closed it |
|---|---|
| `code-semantics-api` was not the API | **Landed `ded6bd0`, 2026-09-06.** The `symbol` and `concept` packages and `PullRequestFacts` — stage contracts of the abandoned concept and behaviour paths, with zero importers outside their own tests — are deleted, and `name`, `model` and the surviving `repository` classes are folded into `code-semantics-engine` under their own package names, so no consumer import moved. `WordMorphology` moved rather than died: `WordSegmenter` still reaches it, and row 6 keeps its measurement. [`THE_API_MODULE.md`](docs/plans/THE_API_MODULE.md) holds the consumer counts that decided it |
| `TaxonomyShape` is dead, and the rule it states no longer matches the files | **Deleted `7baacf5`, 2026-09-06.** Nothing called it, and its rule matched one of the seven vocabularies it claimed to classify |
| A bundled resource's header is written twice and only one copy is checked | **Landed `f615ff9`, 2026-09-06.** Each SKOS extraction renders through one `rendered()` call and `RenderedResourceTest` re-renders all eleven taxonomies from `SkosRows`' own read-back, asserting byte equality with the committed file. The first run found the arm-to-path rename stale in the committed FIBO header |
| `PublishedPaths` and `StatedPaths` walk the same tree twice | **Landed `7baacf5`, 2026-09-06, measured first.** The walks differ on 3,915 of 24,436 concepts by a deliberate rule — the page draws the filing over a stated chain and the export reads it only under an unplaced concept — so the deletion the row proposed was wrong. `PublishedPaths` now delegates the chain, the field levels and the module to `StatedAncestry` and `StatedPaths`, keeping that one rule; 24,424 paths are unchanged and the 12 that moved are BIAN rows whose chain tops out in its own label, gaining the business domain their publisher states |
| The extractors | **Landed `85d9adb`, 2026-09-04.** Six steps stated once instead of once per source: `PinnedSet` replaced seven hand-written digest acceptances, `BundledResource` fifteen copies of the write, `StatedSources` sixteen `main` methods and their usage strings, and a `BundledExtraction` record an enum of eighteen anonymous subclasses. It was the gate in front of rows 15, 16 and 20, and that gate is lifted. **Settled by** `sql-functions`, the one source needing neither a network route nor a local copy: regenerated through the new path, `git diff` over its TSV reports nothing. The other seventeen rest on the constants, every one of which moved unchanged and is the value its committed header carries |
| Two terms CLAUDE.md refuses are still in the tree | **Landed `347cdd3` and `ef41ded`, 2026-09-04.** Three went, not two. **`rung`** carried three meanings across 88 files and each becomes a word the export already publishes: `TermRung` is `MatchNormalisation`, `AnswerRungs` is `EvidenceKinds`, and a depth in a publisher's tree is a level. `ladder`, the metaphor it belonged to, went with it. **`arm`** carried two: the manifest's last column is `expected-result` with the ordinary controls CLAUDE.md names — seven positive, four negative — and the reading's own arms are paths, so `ArmPlacementProbe` is `PathPlacementProbe` and the task is `pathPlacement`. **`panel`** went with it, and `THE_PANEL.md` is [`THE_EVALUATION_SET.md`](docs/plans/THE_EVALUATION_SET.md). `evidence.json` rises to 6.0 because `TermMatch.rung` is a key it states; `reading.json` is untouched at 25.0, because none of the three was ever a published field. CLAUDE.md's table now reads as three refused terms rather than two in the tree |
| A package namespace that matches the published coordinate | **Landed `fd6d701`, 2026-09-04.** The 232 files the port brought with it move from `io.github.fiftieshousewife.bi.lexicon` to `codesemantics.lexicon` and `codesemantics.lexicon.extraction`, which is the `codesemantics.<concern>` shape every other module already has. **No resource path moved with them**: every `getResourceAsStream` in the two modules is `/`-prefixed and the resources sit at the resource root, and `VocabularyProvenanceTest` and `BundledResourceReachabilityTest` were run on their own afterwards, reporting 1 and 3 assertions with no skips. `lexicon-extraction`'s `mainClass`, the README's link to `CsoSubjects` and the import blocks of 73 files moved with it |
| `WordSpecificity` gives the commonest word a weight of exactly zero | **Landed `e61773f`, 2026-09-03.** `log(1)` is 0, so `the` voted zero while the javadoc stated a weight that is never a gate — and a weight of exactly zero is a gate. The scale is `log(rank + 1) / log(size + 1)`, which is what `CorpusSpecificity` already reads over the corpus for the reason its own javadoc gives. The class had no test and now has four. `PhraseSpecificity` loses the property that `the name` scores exactly what `name` scores, which rested on the zero. **Backtested on the eleven members, criterion stated before the run**: 59 of 66 stand apart before and after, mean margin 0.0350 both, leader in the area 5/11 at both levels, no crossings. Three figures moved out of 385, each by a tenth of a point. **The row's premise that it moves every figure in the tree is refuted**: `PhraseTopics` normalises each phrase's shares to one, dividing the per-word weights back out, and `TopicTally.mostNarrowing` takes an argmax a uniform one-rank shift preserves. It stays on the contract argument with no accuracy gain |
| A tree not laid out as `<module>/src/<set>/java` reads as empty and reports no error | **Landed `34a3641` and `2a4a510`, 2026-09-02, at schema 25.0.** `UnreadJavaFiles` counts the Java files under the root that no scope holds, and `setAside.javaFilesNoScopeReached` publishes it. It votes nothing and states coverage. Read by the reader itself: aeron, besu, jmeter-iso8583, jpos, quickfixj, santuario, strata and tika hide 0 each, fineract 1, fix-trading-simulator 2, maven 2 of 3,126, this repository 0 of 884, and `handcoded/fpml-toolkit-java` 195 of 195. A `.java` file under `src/test/resources` is in a scope and is not counted, which is why maven reads 2 where a shell sweep counts 5. **The reading also died rather than reading empty**: no scope means no written word, so every one of the 999 draws was empty and `VocabularyNull.barFor` threw a bare `NoSuchElementException`. The bar now sits at the divergence's own maximum of 1 bit where the draws produced none — a bar of zero admits every word and the maximum admits none. **The row's stated fix is refuted**: `fpml-toolkit-java` carries no build file at all; maven's 20 poms declaring `<sourceDirectory>` are its own test fixtures (`src/should-be-ignored/java`, `src\main\j`, `../sibling/src-main`); and besu, fineract and aeron declare `srcDir` as generated output — `new File(buildDir, "generated/java/src/main/java")`, `build/distributions` — in expressions no static read resolves |
| The description is ordered by evidential rarity, so the vocabulary that describes least comes first | **Landed `a788ef4`, 2026-09-02.** `DrawnReadings.subjectsOf` sorted the pooled subjects by each answering source's own `chanceRate`, which imported the ordering of `answers` into the line a reader reads for meaning. `SubjectDescription` reads whether a publisher states prose for any concept placed under a subject, and the description is ordered by that, then by how the label reads, then by the share of the repository written under it; `answers` keeps the chance rate. Measured over the twelve published readings: 9 of 12 reorder, every reading's answers are unchanged, and one leading subject changes — this repository's, from OLiA's `noun` at 2 occurrences to CSO's `natural language processing` at 24. On jpos BIAN's `cards` rises from 32nd to 9th, which is the case the row named. 27 of the 480 pooled subjects carry no prose from their publisher, and the presence key lifts a described subject above an undescribed one more of the repository sits under 643 times. **`reachedItsArea` moves on none of the twelve**, because it reads the scheme placement and not the description, so the settling measurement the row proposed cannot answer it |
| Ten of the twelve published readings state no commit | **Landed `eebaa28`, 2026-09-01.** `EvaluationReadCommand` passed a hard-coded empty string where `read` passes a commit; the manifest's own `sha` column had pinned every member for exactly this reason, so `commitOf(member)` takes it from there and no `.git` is read. A member pinning no commit now fails rather than being written unnamed |
| The readings page calls a parent a branch and a concept a phrase | **Landed `1a55ee6`, 2026-09-01.** The fold reads *every concept FIBO matched: 5 parents, 30 concepts*, the spans and their rules are named for what they hold, and the lede defines *parent* at first use. A test now asserts the page states a rule for every class it emits, which is the check the page plan named and nothing ran; it was red on arrival, because the `more` link took its only rule from `term-trees.css`, which `readings.html` does not carry |
| FIX and FpML placed against rather than matched | **Refuted 2026-09-01 by the control it was proposed as.** The premise was that FIX and FpML pool to field sizes arXiv and OpenAlex work at where BIAN's 43 was too small. Measured on the bundled extracts: FIX pools to **35** first parents, one of which (`Common`) holds 69% of its described rows, and the 242 the proposal counted are parent *combinations* — 408 FIX rows state several parents, so `Common \| Session` was counted as a subject distinct from `Common`. FpML pools to 147, and 694 of its 1,181 described rows state no parent at all. Both leaf levels were then placed against all eleven evaluation members with `functionalPlacement`. **FIX stands apart on 5 of 11** — QuickFIX/J, fix-trading-simulator and jmeter-iso8583, and also **Santuario and Tika**, while reaching neither Strata, Fineract nor jPOS. **FpML stands apart on 2 of 11** — fix-trading-simulator, and **Aeron** by a tenth of a point, while not reaching Strata, its own field. Neither field size nor prose per subject explains it: FIX's leaves carry a median of 12 words and FpML's 14, and the two behave differently. This is the third and fourth time a term vocabulary has failed the out-of-domain placement control. **Do not re-propose without a mechanism that predicts which of the two behaves how** |
| `./gradlew read` fails on a stale pinned figure | **Landed `ad8c449`, 2026-08-31.** `ReachedSubjectTest` pinned tika's leading branch at 11 written concepts and it is 10: CSO published `parsing` and `parsing_algorithm` as two rows, each naming the other as an equivalent label, and `ccfd135` collapsed them. The vocabulary lost a duplicate and the pin moved to 10. The diagnostic now names the concepts a branch's count rests on, and the spellings the pooling folds |
| `FpmlConcepts` says naming a base is not climbing to it, and `StatedAncestry` climbs to it | **Landed `7d92178`, 2026-08-31.** FpML declares `Scheme` and `NonEmptyScheme` as `xsd:simpleType`, and a base the schema set declares a simple type is a format constraint rather than a subject, so the extraction states it in `note` and it never reaches the walk. Checked over the published readings on 2026-09-01: no concept row is grouped under either. A base the set declares nothing at all for is still carried in `broader`, which is what keeps FIBO's 228 rows under the OMG Commons Ontology Library's `PartyRole` and `Classifier` |
| `fpml-terms.tsv`'s header describes behaviour the extraction changed | **Closed by the same change.** The header and `FpmlConcepts`'s javadoc now state the same rule |
| `atTheTopOfItsBranch` is empty where a publisher places a concept nowhere | **Landed 2026-08-30 and regenerated.** `StatedAncestry.pathAbove` returns the whole walk and `ExportedTaxonomy.Concept` carries `statedPath` in its place, at schema 19.0 |
| The ranking favours the largest vocabulary | **Landed 2026-08-31 at schema 22.0, and it did not do what was expected.** `ExportedTaxonomy.Bar` carries `chanceRate` — `(atLeastAsExtreme + 1) / (resamples + 1)`, bounded in `[1/1000, 1]` by the estimator's own definition — and `DrawnReadings` ranks by it, ties broken by the count past the bar. On jpos: CSO 0.0010, FIBO 0.0080, FpML 0.0310, BIAN 0.0330, FIX 0.0380. CSO's rate is at the floor, so jpos leading with `public key cryptography` is not a ranking artefact |
| A standard names itself where that distinguishes nothing | **Landed 2026-08-31.** `DrawnReadings.subjectsAMajorityNames` derives the bound the way `StatedAncestry.fieldLevels` does and refuses CSO alone, because FpML, FIX and FIBO answer five readings each. Narrow standards are untouched, and three readings now state no standards line at all |
| The export counts the vocabularies it set aside without naming them | **Landed 2026-08-31 at schema 21.0.** `setAside.vocabulariesBelowTheirChanceBar` is a row per refused vocabulary carrying the publisher's name, the count it reached, the count a deal of its own words reaches, the median, the field and the resamples, sharing one `$defs/chanceBar` with `taxonomies[].bar` |
| Favour the nodes with the better descriptions | **Refuted by its own measurement, 2026-08-31.** Length is not quality: FIBO's definitions add a median of 17 words and are genus-and-differentia, so a rule preferring length would rank them beneath BIAN's 75 words. Restatement is 2% of FIX, 1% of FpML and 0% elsewhere. And after the CSO abstracts join, 93% of matches are describable, so the ordering would separate 7% of the field |
| FIX and FpML should point to BIAN service domains rather than compete with them | **Researched 2026-08-31 and refused: no publisher states the mapping.** BIAN publishes one external cross-reference and it is attribute to ISO type, never message to service domain; `bian-official/artefacts` names no external standard in any of its 1,292 CSVs; the FIX Trading Community's FIX↔ISO 20022 mapping is ISO/TR 22126-5:2022, which ISO sells. The granularity would kill it even with the link in hand, since `CurrencyAndAmount` and `ISODateTime` recur across nearly every service domain. **Do not re-propose without a new published artefact** |
| BIAN is prose and is matched as labels | **Built 2026-08-31 and refuted by its own control.** `BianServiceDomains` implements `PublishedSubjects` and `PlacedField.ofBian` places at both levels, and the reading does not publish it: over the eleven members the business-domain level stood apart from chance on nine, and the two it could not separate were fineract and strata, the only banking and derivatives libraries in the set. 319 domains pool into 43, where arXiv states 155 and OpenAlex 4,516. The class and the factory stay and `FunctionalPlacementProbe` draws both levels. This is the second time BIAN's placement has failed its null |
| How to call this as a library | **Landed 2026-08-30.** `ExportedReading.of(Path, String)` reads a directory and returns the export, and the README states the call and the coordinate |
| The read spends four fifths of itself assembling the export | **Landed 2026-08-30.** `ContentWords.lemmaOf`, `WordNetSenses.entry` and `LongestRuns` each ask their question once where they asked it repeatedly: 222.7 s to 123.8 s on this repository, 44% faster with no figure moved, shown by reading tika before and after and diffing. What is left of the cost is row 14 |
| Retire the platform's API reference | **Done 2026-08-24.** `PlatformVocabulary` and `PlatformNames` are deleted and the hundred-repository corpus is the second reference. It moved no figure on the nine |

## Set aside plans

Plans that completed, were refuted by measurement, or were ended by a stated decision are deleted; `git log -- docs/plans/<name>.md` holds each document. Two are kept instead of deleted, each carrying a status line at the top: `ONE_ANSWER.md`, because the open `ONE_PAGE.md` builds on the design it states, and `PHRASE_EVIDENCE.md` with its notes, because it records two refutations this list says are not to be retried. The decision that ended the most of them at once: **the vocabulary is the one path** — the concept and behaviour readings are recorded, exported and never repaired.

| Plan | What ended it |
|---|---|
| `A_VOTE_NOT_A_GATE` | The workings landed in `evidence.json`; the second-pass zero it also named belonged to the term path |
| `BEHAVIOURS`, `WHAT_IT_ACTUALLY_DOES` | The decision to keep one reading path. No verbal taxonomy exists to match, and the behaviour reading left the export |
| `CLEANUP_AND_UPLIFT` | Discharged; the parked chance-field decision is row 14 |
| `DESCRIBING_A_MATCH` | **Complete at schema 24.0.** `StatedDescriptions` climbs `StatedAncestry.pathAbove`, so the majority-level refusal needed no rule of its own; rows carry `description` and `descriptionStatedFor`, `definition` is untouched and no answer moved. It reproduced its own survey within one concept. Its three open questions are row 5 |
| `EVERY_SIGNAL_REACHES_A_SUBJECT` | Step 1 landed; steps 2–4 asked the concept and behaviour readings to place, and the decision to keep one reading path ended that |
| `JSON_ONLY_OUTPUT` | Complete: `reading.json`, `evidence.json` and `changes.json` are the output |
| `OLIA_DEFINITIONS` | The extraction landed; the pages that showed it went with the term path's reports |
| `ONE_ANSWER` — **kept** | **Complete.** Steps 1–5 landed through 2026-08-30 — the pooled run table, `SpecificTerms` on the index, the bar in `reading.json` with what falls below it withheld and counted, the cascade in `summary.answers` at schema 17.0, and `ExportedReading.of` as the entry point. Step 6 was measured to a conclusion on all eleven members and is row 4; the single-word question it raised is row 3 |
| `ONE_ARM` | Its word-filtering half continues in `POOL_THE_CORPUS.md`; its condensation of the readings ended with the decision to keep one reading path |
| `PHRASE_EVIDENCE`, `PHRASE_EVIDENCE_NOTES` — **kept** | Parts 1, 2 and 3b landed and were measured on all ten readings; the bar permutes the **vocabulary** — `ScrambledTerms` deals a source's own words across its own terms — after permuting the repository was measured and refuted twice. Part 3a was built, measured on the nine and refuted: the published term as a shared unit gained on the vocabulary ranking and took the placement from 33 of 36 level readings standing apart to 22. `ONE_ANSWER.md` continued it. **Do not retry**: refusing a term another bundled vocabulary also states — 95 to 99.9% of every vocabulary's multi-word terms are stated by no other of the seven |
| `PHRASE_MATCHED_SUBJECTS`, `PHRASE_ARM_REPAIRS` | The reading was built and read below chance on tika; repairs 0, 3 and 4 were measured and refused |
| `RANK_DO_NOT_GATE` | Steps 1–3 closed; continued from step 4 by `POOL_THE_CORPUS.md` |
| `RELEVANT_SUBJECTS` | The subtree walk's defects belonged to the term path |
| `SENSE_DISAMBIGUATION` | Superseded by `AGREED_SENSES.md` |
| `SUBJECT_CONDITIONED_MATCHING` | Conditioning the matcher ended with the term path; the scheme decision survives as row 23 |
| `TERM_MATCHING` | Depth was measured and refuted |
| `THE_COUNT_THAT_CLEARED_THE_BAR` | **Complete at schema 23.0.** Every published row states the level it was matched at, `bar.phrases` says in javadoc and schema that it counts the `words` level, and the page separates the two. The decisive check holds on 27 of 28 vocabulary-and-reading pairs and falls one short on fineract's CSO, 30 against 31 — **the ladder runs both ways**, and `ReadingExportDiagnostic` asserts the bound rather than equality. `./gradlew countedPhrases -Dcs.clone.dir=<path>` names the difference. Both repairs stay refused: a four-level null measures the dictionary rather than the repository and costs 81,689,229 normalisations per reading, and publishing only what the bar counted discards real matches |
| `THE_PIPELINE` | The ported project's pipeline; this library reads a clone path |
| `THE_REPORTS` | There are no reports |
| `UNREAD_SIGNALS` | All five ranked items landed, each with its measurement |
| `VISUALISATION` | Superseded by `ONE_PAGE.md` |
| `WIKTIONARY_HIERARCHY` | The fold landed; the rest needs a published mapping between two label spaces, and none exists |
