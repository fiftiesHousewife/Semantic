# What the reading shows — CodeSemantics

Every line below cleared a bar that follows from the size of the field it competed in. What did not clear one is named at the end and reported nowhere.

## How much of it could be read

**λ = 0.983** — a bundled resource can be cited for that share of 74,637 word occurrences across 368 files, 71% of them prose. It is the denominator everything else rests on, not a finding.

Of the mass the topical reading then observed, **75.5% of it was settled on no subject**: a phrase nothing could place, or one whose words named so many subjects that none of them was settled. That share is in the denominator of every ι reported anywhere, so a topic's intensity is a share of everything written and not of the little that resolved.

## The field it is in

**Computer Science**, 0.3658 bits away. A taxonomy of chance placed its nearest subject at 0.4239 bits over 999 draws, so this stands apart from chance. The runner-up is Electrical Engineering and Systems Science at 0.5046.

## What it is about

The topics that make some part of this repository unlike the rest of it, commonest first: `linguistics`, `computing`, `grammar`.

A topic held at the same density everywhere distinguishes nothing, so there is no ranking of the repository as a whole here — a ranking has nothing to be compared against.

## What distinguishes each part

| Scope | Bits from the repository | Writes more of |
|---|--:|---|
| `code-semantics-api/src/test/java` | 0.1932 | `linguistics` |
| `code-semantics-engine/src/main/java` | 0.0660 | `grammar`, `linguistics` |
| `lexicon/src/main/java` | 0.1491 | `grammar`, `linguistics` |
| `lexicon/src/test/java` | 0.1479 | `linguistics` |

## Withheld

- `documentation` — 0.2722 bits, and 810 of 999 chance draws stood at least as far
