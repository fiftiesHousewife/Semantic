# What the reading shows — tika

Every figure below exceeds all 999 resamples of its own size drawn from this same repository. **Withheld** at the end names what did not.

## How much of it could be read

| | |
|---|--:|
| Word occurrences read | 770,028 |
| Files | 2,156 |
| Prose share of those occurrences | 78% |
| **λ** — share a bundled resource has an entry for | **0.972** |
| Evidence resolving to no subject | 68.7% |

λ is the denominator every figure below rests on. The second share counts something else: a phrase no resource places, or one whose words name so many subjects that none of them resolves. A word can have a dictionary entry and still say nothing about subject matter, so both shares run high together.

## The field it is in

| Level | Nearest subject | Distance | Nearest by chance | |
|---|---|--:|--:|---|
| Archive | **Computer Science** | 0.3621 | 0.4337 | apart from chance |
| Category | **Computation and Language** | 0.3817 | 0.4375 | apart from chance |

The archive is compared against every category's description pooled under it, which is enough prose for the divergence to be stable. The category is compared against the few dozen words the scheme states for it alone, which is the weaker measurement and the sharper answer. The archive behind the leading one is Electrical Engineering and Systems Science at 0.3871.

## What it is about

The topics distinguishing some part of this repository from the rest, commonest first: `computing`, `law`, `linguistics`.

A topic written at the same density everywhere distinguishes no part of it, so this list ranks nothing about the repository as a whole.

## What distinguishes each part

| Scope | Bits from the repository | Writes more of |
|---|--:|---|
| `tika-integration-tests/tika-pipes-solr-integration-tests/src/test/java` | 0.4431 | `law` |
| `tika-langdetect/tika-langdetect-charsoup-core/src/main/java` | 0.3296 | `linguistics` |
| `tika-translate/src/test/java` | 0.3106 | `linguistics` |
| `tika-metadata-schema/src/test/java` | 0.2883 | `law` |
| `tika-pipes/tika-pipes-plugins/tika-pipes-microsoft-graph/src/main/java` | 0.2749 | `law` |
| `documentation` | 0.2534 | `law`, `linguistics` |
| `tika-pipes/tika-pipes-plugins/tika-pipes-solr/src/main/java` | 0.2386 | `law` |
| `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-image-module/src/test/java` | 0.2353 | `computing` |

22 more departed further than chance — `themes.md` names every one.

## Withheld

- `tika-annotation-processor/src/main/java` — 0.2750 bits, and 240 of 999 chance draws stood at least as far
- `tika-annotation-processor/src/test/java` — 0.5560 bits, and 134 of 999 chance draws stood at least as far
- `tika-app/src/main/java` — 0.1499 bits, and 840 of 999 chance draws stood at least as far
- `tika-app/src/test/java` — 0.2286 bits, and 303 of 999 chance draws stood at least as far
- `tika-bundles/tika-bundle-standard/src/main/java` — 0.3320 bits, and 652 of 999 chance draws stood at least as far
- `tika-bundles/tika-bundle-standard/src/test/java` — 0.3985 bits, and 407 of 999 chance draws stood at least as far
- `tika-detectors/tika-detector-magika/src/main/java` — 0.2934 bits, and 809 of 999 chance draws stood at least as far
- `tika-detectors/tika-detector-magika/src/test/java` — 0.2842 bits, and 470 of 999 chance draws stood at least as far

133 more fell within chance as well — `themes.md` names every one.
