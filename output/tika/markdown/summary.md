# What the reading shows — tika

Every figure below exceeds all 999 resamples of its own size drawn from this same repository. **Withheld** at the end names what did not.

## How much of it could be read

| | |
|---|--:|
| Word occurrences read | 768,990 |
| Files | 2,156 |
| Prose share of those occurrences | 78% |
| **λ** — share a bundled resource has an entry for | **0.972** |
| Evidence resolving to no subject | 66.4% |

λ is the denominator every figure below rests on. The second share counts something else: a phrase no resource places, or one whose words name so many subjects that none of them resolves. A word can have a dictionary entry and still say nothing about subject matter, so both shares run high together.

## The field it is in

| Level | Nearest subject | Divergence | Chance reaches | |
|---|---|--:|--:|---|
| Archive | **Computer Science** | 36.2% | 42.3% | apart from chance |
| Category | **Computation and Language** | 38.5% | 43.0% | apart from chance |

A divergence is written as the share of its own maximum it holds: 0% is two readings that are identical, 100% two sharing no subject at all. The archive is compared against every category's description pooled under it, which is enough prose for the divergence to be stable. The category is compared against the few dozen words the scheme states for it alone, which is the weaker measurement and the sharper answer. The archive behind the leading one is Electrical Engineering and Systems Science at 37.5%.

## What it is about

The topics distinguishing some part of this repository from the rest, commonest first: `computing`, `linguistics`.

A topic written at the same density everywhere distinguishes no part of it, so this list ranks nothing about the repository as a whole.

## What distinguishes each part

| Scope | Divergence from the repository | Writes more of |
|---|--:|---|
| `tika-translate/src/test/java` | 41.1% | `linguistics` |
| `documentation` | 31.0% | `linguistics` |
| `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-image-module/src/test/java` | 27.5% | `computing` |
| `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-xml-module/src/main/java` | 23.0% | `computing` |
| `tika-pipes/tika-pipes-plugins/tika-pipes-file-system/src/main/java` | 22.5% | `computing` |
| `tika-server/tika-server-standard/src/test/java` | 21.7% | `computing` |
| `tika-parsers/tika-parsers-standard/tika-parsers-standard-modules/tika-parser-zip-commons/src/main/java` | 21.2% | `linguistics` |
| `tika-langdetect/tika-langdetect-charsoup/src/test/java` | 20.3% | `linguistics` |

9 more departed further than chance — `themes.md` names every one.

## Withheld

- `tika-annotation-processor/src/main/java` — 35.7% of the maximum divergence, and 211 of 999 chance draws stood at least as far
- `tika-annotation-processor/src/test/java` — 67.5% of the maximum divergence, and 136 of 999 chance draws stood at least as far
- `tika-app/src/main/java` — 13.8% of the maximum divergence, and 986 of 999 chance draws stood at least as far
- `tika-app/src/test/java` — 22.4% of the maximum divergence, and 727 of 999 chance draws stood at least as far
- `tika-bundles/tika-bundle-standard/src/main/java` — 42.6% of the maximum divergence, and 493 of 999 chance draws stood at least as far
- `tika-bundles/tika-bundle-standard/src/test/java` — 41.1% of the maximum divergence, and 553 of 999 chance draws stood at least as far
- `tika-detectors/tika-detector-magika/src/main/java` — 30.2% of the maximum divergence, and 860 of 999 chance draws stood at least as far
- `tika-detectors/tika-detector-magika/src/test/java` — 40.8% of the maximum divergence, and 262 of 999 chance draws stood at least as far

137 more fell within chance as well — `themes.md` names every one.
