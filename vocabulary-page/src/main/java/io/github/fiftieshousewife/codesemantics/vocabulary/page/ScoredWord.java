package io.github.fiftieshousewife.codesemantics.vocabulary.page;

/**
 * One significant word offered to a picture: as the splitter produced it, with its claim in bits — its
 * term of the divergence against the reference scoring it lowest, the figure every page sizes by.
 */
public record ScoredWord(String word, double claim) {
}
